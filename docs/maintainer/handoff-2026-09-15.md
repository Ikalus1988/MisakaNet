# Handoff — 2026-09-15（根包 2.30.0 发布、两份外部评审核对、两处静默漂移修复）

> 上一份：`handoff-2026-09-14.md`（npm 发布走通、CodeQL 清零、agent 自启动、OpenClaw 接入）。
> 本文件只记 09-15 这一轮，并**明确区分"已验证"与"待执行/待实测"**。
> 评审逐条核对见 `docs/maintainer/review-assessment-2026-09-15.md`。

## 1. 本轮交付（按时间顺序）

| 提交 | 内容 |
|---|---|
| `c8c38dcb2` | **修两处静默漂移**：README Glama 段的 `385+ **verified failure-recovery lessons**` → `393+ **indexed** …`；JOIN.md Version-Info 的 `384+ lessons` / `v2.29.0`。两句此前**都不在任何门禁里**，已注册进 `sync_lesson_count.py`，并把版本行用 `align_versions.py --registry` 对齐 |
| `c8c38dcb2` | **补词表门禁的洞**：`FORBIDDEN_TRUST_CLAIM` 原为 `verified (failure\|debugging) lessons?`——**匹配不到连字符形式**，所以那句违反 `trust-semantics.md` 的句子能在测试下全绿；已收口为 `(?:-recovery)?`。回灌验证：把两处漂移放回去，`--check` 精确报出 `README.md:275` / `JOIN.md:186` 并 `exit 1` |
| `c8c38dcb2` | `docs/llms.txt` + 其 `.well-known` 副本：`queries that return verified lessons` → `indexed` |
| `fd67ee644` | **handoff P0.2**：`package.json` 的 `files` 加入 `.codex-plugin/**`（tarball 10 → 14 文件、37.3 kB → 1.0 MB） |
| `22936823b` | 评审评估文档（9 条逐条核对 + 真问题清单） |
| — | **handoff P0.1：正式发布 `misakanet@2.30.0`**（tag `misakanet-v2.30.0` → `misakanet-publish.yml` → registry 已更新） |

## 2. 已核实的线上事实（别再猜）

- **`misakanet@2.30.0` 已发布**，且**与本地 `npm pack` 逐字节一致**：
  `dist.shasum = 5384d44e0442e5b0d826abba2fd204a43b119334` = 本地 dry-run 的 shasum；
  14 文件 / 1,090,295 B 解包。此前 registry 停在 **2.23.0**（本次是 7 个版本的空档一次补齐）。
- **PyPI 上 `misakanet` 存在且已是 2.30.0**（外部评审称"根本不存在 PyPI 上"是错的）；
  **`misakanet-core` 2.7.0 的 `requires_dist = None`**——它自己就是零依赖，
  但它是本仓的**硬依赖**（`requirements.txt:3`、`pyproject.toml:13`）。"Zero dependencies" 的宣传口径
  需要一句限定：*免的是向量库/嵌入模型/服务器，不是 `pip install misakanet-core`*。
- **MCP 工具面（实测）**：远端 `https://misakanet.org/mcp` = **7** 个工具（与 README / AGENTS.md §3.2 一致）；
  本地 stdio = **9** 个（多 `submit_usage` / `usage_status` / `memory_context`，少 `me_events`）。
  `submit_usage` / `usage_status` **未部署到 worker**，`docs/mcp-eval-20260903.md:150` 早已记录这个分叉。
- **索引口径**：`data/lessons.json` = **393** 条 = `contrib` 349 + `en` 32 + `core` 10 + `user-rescue` 2。
  `lessons/**/*.md` 全量是 440（含 templates、根目录散落文件、其余翻译目录、未索引的 en）——
  **"indexed" 与"文件数"不是同一个量**，README 用的正是 "indexed"。
- **本机 pytest 全量**（排除两个 mcp 依赖模块）：**1196 passed, 3 failed, 15 skipped**。
  3 个失败**全部是本地环境**：`tests/test_intake_kind.py` ×2 与 `test_mcp_http_server.py` /
  `test_intake_spam_guard.py` 的收集错误同因——本机 `mcp` 是 **1.28.1**，`requirements.txt` 要 `>=2.2.0`
  （少了 `mcp.server.mcpserver` / `mcp.Client`）；另一个是 embedding 服务（CUDA/joblib 权限）。
  **要在本机跑绿，需 `pip install -U 'mcp>=2.2.0'`。**

## 3. 本轮踩的坑

1. **版本门禁只挡"文档超前"，落后是静默的**（`align_versions.py` 的 R5）。`--source 2.30.0` 不动
   `API.md` / `JOIN.md`（那是 `--registry` 的职责），于是它们安静地落后两个版本，`--check` 仍然全绿。
2. **一条"违反词表"的句子能在词表测试下全绿**：正则少了连字符分支。**门禁的覆盖面比它的名字窄**——
   这是这个仓库第二次栽在同类问题上（第一次是 `{{LESSONS_COUNT}}` 一次性替换）。
3. **没被注册的句子 = 无人看守的漂移面**。README 的 Glama 段、JOIN 的 Version-Info 块都"在公开面上、
   有数字、没进 SSOT"，所以只能靠人偶然看见。
4. 本机 `npm` 必须 `--cache /tmp/...`（`$HOME` 只读，旧缓存里有 root 文件）。
5. **未认证的 GitHub API 很容易被限流**（本轮用它查 workflow 结论时连续返回空）——
   验证发布状态更可靠的办法是**直接查 registry**（`npm view` / `dist.shasum`）。
6. 本仓是 **shallow clone**：`git rev-parse --is-shallow-repository` = true，`git log --all` 看不到
   09-07 边界之前的主线历史（做历史取证时必须声明这一点）。
7. 外部评审这份"实测读代码"的报告里，**四条硬错全部来自"读到的不是 HEAD"**：gitignored 的
   `.pnpm-store` 快照、别人的示例文本、修复前的代码、历史报告。仓库侧的对策见 §4 第 1 条。

## 4. 待办

### P0 —— 已完成
1. ~~发布根包 `misakanet@2.30.0`~~ ✅
2. ~~`.codex-plugin/**` 加进根包 `files`~~ ✅

### P1 —— 本机实测 / 代码补齐
3. 在 **openclaw / hermes / claude-haha** 上真跑安装器：`--dry-run` → 真装 → `--verify` →
   `openclaw mcp list` / `hermes mcp list` 核对；沙箱内先 `export XDG_CACHE_HOME=/tmp/openclaw-cache`。
4. **Node npx CLI 还没有 OpenClaw 目标**：`packages/misakanet-setup/bin/misakanet-setup.mjs:55`
   仍是 `AGENTS = ['claude','codex','hermes']`，而 Python 安装器
   （`integrations/agent-autostart/install_misakanet_agent.py:56`）已是
   `claude, codex, hermes, openclaw, dsh`——**npx 用户拿不到 OpenClaw**，需要 parity（含 verify/uninstall 与测试）。
5. hermes 钩子：payload 形状与 CC 不同，需实测适配（`MISAKANET_HOOK_DEBUG=1`），不要假设可即插即用。

### P2 —— 评审顺手指出的真问题（本轮未改，按价值排序）
6. **`misakanet/server/tools.py:181-183` 的过期描述**：仍写 "Side effects: currently returns a local
   placeholder report only."（真实副作用会写 `helpful:<id>` KV）。这是 **agent 面的事实错误**，
   且极可能就是那份评审"sient no-op"结论的来源。
7. **远端工具面在文档里不一致**：`docs/integrations/mcp-remote.md:268,272` 把未部署的工具列为远端可用；
   `docs/mcp.md:70-73` 只列 3 个远端工具（实际 7）。
8. **`README.zh-CN.md:11`** 硬编码 badge `nodes-59` 与 `lessons-358`（无刷新机制）。
9. **domain 词表治理**：68 种原始写法 / 61 种归一；`lesson_gate.py:202-220` 白名单自放宽；
   `queue_lesson.py:350` 的 `--domain` 自由文本；`data/synonyms.json` 是无人读取的死副本。
10. **provenance 回填同质化**：`evidence: post-publication` ×384、`source: community` ×289——
    README:496 的"渠道级"来源声明目前不可核验，要么补证据要么改措辞。
11. 继承上一份：PR #1656 / #1646 / #1604 / #1544；intake 6 条待补细节、12 条 zero-bounty；
    **#1675**（search 命中课程 `problem/fix` 恒为空）；npm 令牌轮换；两个包的渠道说明。

## 5. 教训（一句话版）

- **门禁的名字不等于它的覆盖面**：正则少一个分支、SSOT 少注册一句，都会让"绿"失去意义。
  修完要**回灌验证**（把漂移放回去，看它是否真的红）。
- **数字类表面要么进 SSOT，要么标快照日期**，否则它迟早被外部读者当成现状引用——本轮两份评审就是证据。
- **验证发布以 registry 为准**（`npm view … dist.shasum` 对本地 pack 的 shasum），不要只信 CI 的绿色。
- **"实测读代码"必须锚定 HEAD**：`git rev-parse --short HEAD` 写进结论里，才不会把快照当现状。

---

## 6. 同日稍后：两个扫描器告警、0.2.1、以及三个操作性新事实

### 6.1 交付

| 提交 | 内容 |
|---|---|
| #1684 → `1701e36d4` | npx 安装器补 **OpenClaw 目标**（规则块 + MCP），verify/uninstall 覆盖，测试 5 条 |
| #1686 → `c304e2178` | **节点计数并入计数 SSOT** + `node_status.py --mirror` + 每日 `sync-node-counter.yml`；README.zh-CN 硬编码 badge 改 endpoint badge |
| #1688 → `befd320fa` | **删掉两条"文件数据外流"流向**（token 进 argv、client_id 从文件读后外发） |
| #1690 → `c5bc60940e` | **User-Agent 版本改回字面量**并由测试绑定 manifest；#1689 因自身引入 high 告警而作废 |
| — | 发布：根包 `misakanet@2.30.0`（shasum `5384d44e…`）、`@misaka-net/misakanet-setup@0.2.0`（`8d167b2f…`）→ **0.2.1** |

开放 CodeQL/扫描器告警：**0**（#269 `SHELL_INJECTION_PATTERN` fixed 05:07:07Z；#268 `js/file-access-to-http` fixed 05:23:01Z）。

新开 issue：**#1685**（`pr-thank-you.yml` 脚本写了不存在的 `github.event`，每个合并的 PR 都红）、**#1687**（domain 计数 18/69/61 三种口径，属产品决定）。

### 6.2 三个操作性新事实（都踩过）

1. **PR 的 `CodeQL` 检查是绿的，不代表告警关了。** #1688 合并后 #268 仍 open，实例被钉在合并提交上、行号从 167 变成 **177**——流向只是**换了一条**（我把版本改成读 `package.json`，而它进了 `User-Agent` 头）。**要判断告警是否消失，只能看合并提交（`refs/heads/main`）上的告警状态**，PR 检查只比"新增"。
2. **用 API 建的 tag 不会触发 `on: push: tags` 工作流**（`POST /git/refs` 返回 201，但 `misakanet-setup-publish.yml` 没有排队）。发布要么用 **git 推 tag**，要么用该工作流的 **`workflow_dispatch`**（它支持"手动发布当前版本"）。
3. **`scripts/gh_push_via_api.py` 只接受"分支 == base"**：它拒绝把一个已存在的 PR 分支往前推（`refusing to move …: it is at <sha>, not the base`）。要往已有的 PR 分支追加提交，只能用 git；git 不通时，**换一个新分支重开 PR 并注明取代关系**（本轮 #1689 → #1690 就是这么处理的）。

### 6.3 又一个"守护自己变成告警"

#1689 的 CodeQL 检查红在 **1 new alert (high)**：为"防止 #268 回来"写的那条测试用
`declared.replace(/\./g, '\\.')` 构造 RegExp——只转义点、不转义反斜杠，正是
`js/incomplete-sanitization`。改成子串比较（`includes`）后消失。
**守卫类改动的自检标准：它会失败吗？它自己会不会成为新的失败源？**

### 6.4 本地工具链的两个坑

- **不要把 `git fetch` 的结果接进管道再判 exit code**：`git fetch … | tail -1` 的退出码是 `tail` 的，
  于是"重试循环"每次都当成功 break——本轮因此以为本地 main 是新的，实际落后一整天。
- **本机 git 协议今天基本不可用**（`GnuTLS recv error (-110)` / 连接超时）：内容推送走
  `scripts/gh_push_via_api.py`，**状态与告警一律查 GitHub API**（`git fetch` 不可信时，
  `origin/main` 是陈旧引用）。

### 6.5 待办变化

- P1.4（npx 补 OpenClaw）**已完成**；P1.5（hermes 钩子 payload）并入 **#1681**。
- 新增待办：**#1681**（Hermes MCP 自动注册 + `--verify` READY 语义收紧到全部目标 + Codex 钩子确证）、
  **#1682**（自更新机制，需先定方案）、**#1685**（一行修复）、**#1687**（domain 口径）。
- Python 安装器（`integrations/agent-autostart/install_misakanet_agent.py`）的 OpenClaw 路径**仍把 token 放进 argv**：
  只有 JS 侧被告警，故本轮未改；与 #1681 同批处理更合适。

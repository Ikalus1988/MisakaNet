# MisakaNet Codex handoff — 2026-08-16

Repo: `C:\Users\hp\MisakaNet`
Maintainer: `Ikalus1988`
Purpose: context for a fresh Codex session. Keep this file operational, not narrative.

## Current state

- Branch: `main`
- Latest known commit after README first-success edit: `b68cbe6 docs(readme): streamline first-success path`
- v2.17.0 is released and current repo version is `2.17.0`.
- Working tree should be clean before new work; verify with `git status --short --untracked-files=all`.
- Public site health was clear on 2026-08-16: homepage, health, counter, lessons, journey all 200.
- Lesson count: 289 indexed lessons.

## README / onboarding status

README first screen was intentionally simplified for first successful action:

1. `What is this?` — one-line failure-memory positioning.
2. `Try in 30 seconds` — 3 entry points:
   - `python3 scripts/misakanet_cli.py smoke`
   - `python3 scripts/mcp_server.py`
   - `python3 scripts/mcp_deepseek_adapter.py`
3. `See it in 8 seconds` — GIF.
4. `Contribute in 3 minutes` — smoke, search, submit 5-line failure note.

Do not re-expand README with long release notes or product theory above the fold. The current goal is clone → smoke → search → contribution.

## DeepSeekHarness / dsh-plugin status

Completed integration chain:

- #1038: `scripts/mcp_deepseek_adapter.py` — DeepSeekHarness MCP adapter with 6 `deepseek.recovery.*` tools.
- #1039: root `SKILL.md` — failure-memory skill definition.
- #1040: `scripts/misakanet_cli.py` — `doctor`, `smoke`, `validate` JSON CLI with exit codes.
- #1042: smoke report completed: `docs/reports/deepseek-harness-smoke-2026-08-15.md`.
- #1048: still open for real local DeepSeekHarness runtime field test.

README now has `Agent / Harness integrations` table and GitHub topics include `dsh-plugin` + `deepseek-harness` (topics full 20/20).

## PR state to preserve

Already integrated into main but GitHub PRs show CLOSED rather than merged:

- #1033 implemented from PR: reputation leaderboard (`08ba987`).
- #1027 implemented from PR: fatal-guard smoke (`4b88f98`).
- #1022 implemented from PR: bench fixtures (`927c33e`).

Do not describe these as GitHub-merged PRs in release notes; say "implemented from PR" or cite commit.

Open PRs requiring author action:

- #1029: hybrid weights; conflicts in `engine.py` / `mcp_server.py`; wait for rebase.
- #1023: fatal-guard CLI hardening; conflicts with Windows/smoke changes; wait for rebase.
- #1031: provenance; 304 files; require split into schema-only and later backfill.
- #1044: lesson + leaderboard mixed; require split.
- #1041: PR Genius rule engine + search page mixed; require split and DCO/scope cleanup.

Continue rejecting unless substantially rewritten:

- #955: README damage + command injection risk.
- #941: incomplete voice volume delivery.
- #940: deletes old production tests.

## Issue state / next work

Recently closed or should remain closed with evidence:

- #1011 fatal-guard CI smoke — commit `4b88f98`, smoke test passes.
- #1017 bench fixtures — commit `927c33e`, 5 fixtures verify PASS.
- #908 reputation leaderboard — commit `08ba987`, `/api/insights/reputation-leaderboard` works.

Important open issues:

- #1048: DSH local runtime field test — next best evidence task.
- #1036: PR Genius rule layers — keep scope to rule engine + config; search page separate.
- #1002: provenance — Phase 1 schema only; Phase 2 backfill later; no 304-file PR.
- #1001: hybrid weights — wait for #1029 rebase and reassess need.
- #1010: fatal-guard CLI — wait for #1023 rebase; avoid duplicating #1027 smoke.
- #1049/#1050/#1051/#1052/#1053/#1054: new backlog around scoring, latency, intake, Feishu, dedupe, changelog automation.

## Growth / ops snapshot

Estimated week 2026-08-09..2026-08-15:

- Clones: ~4,200, unique cloners ~450.
- Views: ~700, unique visitors ~150.
- New stars: ~20; about 30% had PR/issue activity.
- New forks: ~10; about 40% had contributor follow-up.
- GitHub-side growth is strong; website/onboarding conversion remains weaker.

Interpretation: not an exposure problem; it is a first-success/actionability problem. Track clone → smoke → search → feedback.

## New strategic insight: preflight guardrails

A separate machine repeated an old RAG-build failure: bulk PDF/RAG indexing in WSL/VRAM caused memory/driver crash/BSOD even though MisakaNet already had matching lessons:

- `lessons/contrib/rag-build-strategy-batch.md`
- `lessons/contrib/chroma-rebuild-no-checkpoint-cn.md`

Root cause is product workflow, not missing knowledge: MisakaNet is still too postmortem-search oriented. Next strategic direction:

> From postmortem lesson search to preflight guardrails.

Potential bounded issues:

1. `misakanet_preflight` MCP tool: intent + commands + environment → risk + matched lessons + guards.
2. `misakanet guard` CLI: command classifier before high-risk execution.
3. Lesson `triggers` metadata for proactive retrieval.
4. RAG build safety profile: sample first, memory budget, batch size, checkpoint, resume test, abort conditions.

If only one is chosen, build a minimal `misakanet_preflight` first.

## Operational commands

Snapshot:

```powershell
python C:\Users\hp\plugins\misakanet-growth\scripts\misakanet_growth.py brief --limit 8
python C:\Users\hp\plugins\misakanet-growth\scripts\misakanet_growth.py site-health --source-root C:\Users\hp\MisakaNet
git status --short --untracked-files=all
```

Core smoke:

```powershell
python scripts/misakanet_cli.py doctor
python scripts/misakanet_cli.py smoke
python scripts/misakanet_cli.py validate
python scripts/lesson_lint.py --lessons-dir lessons --fail-on high
```

## Next recommended actions

1. Run/complete #1048 real DSH local runtime field test and link evidence.
2. Open one small preflight guardrails issue (prefer `misakanet_preflight`) based on the RAG/BSOD incident.
3. Keep README above-the-fold short; measure first-success path rather than adding more features.
4. Triage open PRs by split/rebase policy; do not manually absorb large mixed PRs.

# MisakaNet handoff — 2026-09-03

Repo: `C:\Users\hp\MisakaNet`
Maintainer: `Ikalus1988`
Session: agent pickup (dsh.so L5 web-smoke root-cause + push)

## Current state

- **`origin/main` = `3e920402`**, `v2.23.1` tag = `90b3e135` — both live on remote.
- Plugin structure simplified: MisakaNet is now a plain SKILL library, not a Cordis entry.
- 1 new lesson indexed: `lessons/contrib/dsh-plugin-l5-web-smoke-cordis-duplicate-entry.md`.
- GitHub default branch still shows 2 critical / 2 high Dependabot alerts (pre-existing, unrelated to this session).

## Recent completions (this session)

### dsh.so L5 web-smoke failure — root cause + fix

**Symptom (dsh.so page `https://www.dsh.so/artifact/misakanet/`, test ID `misakanet-20260902T192254Z`, profile `l5-web-smoke`, dsh `0.1.2-alpha.1`):**

```
L5.1_INSTALL_PASSED_SANDBOXED — Sandbox install passed  ✓ Passed
L5.2_WEB_BOOT_READY          — Web boot ready          ✕ Failed
  detail: plugin tree failed to load
L5.3_HTTP_SERVED             — HTTP endpoint served    ✕ Failed  (cascaded)
```

**Root cause:** `cordis.patch.yml` shipped with v2.23.0 declared an inert Cordis insert:

```yaml
- insert:
    - id: misakanet
      name: misakanet
```

The `apply()` was a no-op, so the insert only existed to populate `pluginInventory`. dsh.so's l5-web-smoke harness re-applies the bundle patch through a second path, so the same id is fed to the Cordis Loader twice. dsh `0.1.2-alpha.1` is strict: any second `insert` with an existing id is a fatal startup error (`TypeError: duplicate loader entry id: misakanet`). That surfaces on the verdict page as the literal "plugin tree failed to load" detail.

Locally reproduced with `DSH_HOME=/tmp/dsh-home-...` against the v2.23.0 tarball; same stack.

**Fix (commit `199c9746`, tag `v2.23.1`):**

| File | Change |
|---|---|
| `package.json` | Drop `dsh.bundle.patch` + `dsh.client.platform` (no client bundle ever shipped). Bump version to `2.23.1`. |
| `cordis.patch.yml` | Deleted. |
| `index.js` | Keep `name` export and the no-op `apply()` so `dsh plugin add` still passes the install contract. Rewrite the top comment to explain why no Cordis contribution is made. |
| `index.d.ts` | Unchanged (already minimal). |
| `lessons/contrib/dsh-plugin-l5-web-smoke-cordis-duplicate-entry.md` | New lesson, full root-cause table + verification block. |

After the change, the plugin is installable via `dsh plugin add misakanet` (npm) or `dsh plugin --profile <name> add github:Ikalus1988/MisakaNet#v2.23.1` (git), but never becomes a Loader row — duplicate-id collisions are structurally impossible.

**Local verification (re-run after the merge):**

```
patch_removed=ok
version=2.23.1
bundle_stripped=ok
dsh_rows=0            (no misakanet entry in dsh --dump-config output)
boot=ok               (dsh --profile web --help exits 0)
root=200              (curl http://127.0.0.1:<port>/  after `dsh web` boots)
```

### Merge + push (commit `3e920402`)

Remote was 13 commits ahead (`a6731d7c` and earlier) — all `chore(data)` / `chore: update leaderboard` / `docs` / `tests` / one `register: Misaka10069`. None touched the four files in the fix, so a `git merge origin/main --no-ff` resolved with zero conflicts. Push was fast-forward from remote's perspective (`a6731d7c..3e920402  main -> main`), no `--force`. Tag `v2.23.1` pushed separately.

### Lesson added

`lessons/contrib/dsh-plugin-l5-web-smoke-cordis-duplicate-entry.md` — passed `validate_lessons.py`, `lesson_gate.py`, and `check_lesson_quality.py`. One soft warning remains (`grep -c` flagged as placeholder-like) which is a false positive — the count is real and is what the test asserts.

## Next roadmap

### P0: Trigger dsh.so re-verdict against `v2.23.1`

dsh.so doesn't auto-rerun on tag push (verified by reading the badge/scan pages — they're snapshot-based). The plugin submission tracker (`https://github.com/ihuajiu/dsh-plugin-submissions`) is the public mechanism; we have no write access in this session. Action item:

1. Visit `https://www.dsh.so/submit/` and re-trigger l5-web-smoke against `github:Ikalus1988/MisakaNet#v2.23.1`, OR
2. File a comment on `ihuajiu/dsh-plugin-submissions` requesting a re-run.

Expected: L5.2 and L5.3 flip to ✓ Passed; L1–L4 stay green; L5 overall verdict flips to Passed; `L5 badge` URL becomes available for the README.

### P1: Dependabot cleanup (carry-over, not blocking)

`security/dependabot` lists 2 critical + 2 high on the default branch. None of them touch the dsh plugin. Worth a sweep in the next session — likely the same `wrangler ^4.127.0` line + a transitive `sharp` (already pinned via `overrides`).

### P2: dsh plugin upgrade notes (defer)

Now that MisakaNet ships without `dsh.bundle.patch`, the `dsh.so` install command should be updated on the artifact page (currently pinned to `#v2.23.0`). The fix is purely server-side at dsh.so; we don't control it. If the page keeps showing v2.23.0 after re-verdict, raise it in the submission tracker.

## Open PRs needing action

None added this session. Pre-existing from `handoff-2026-08-19.md` (#1059 blocker, #1060 DCO, #1041/#1031/#1029/#1023 conflicts) still need a maintainer pass.

## Labels for agent pickup

No new labels added. Existing `agent-friendly` / `status:competition` items unchanged.

## Reusable pattern (for the next agent)

When dsh.so reports a verdict with text like:

- "plugin tree failed to load" → likely Cordis Loader duplicate id. Strip `dsh.bundle.patch` or rename the entry id.
- "Sandbox install failed" → likely network / GitHub codeload timeout in the sandbox. Prefer the npm channel.
- "Web boot ready" failed with no further detail → check `dsh web` stderr first, then re-run with `dsh --profile web --help`.
- "HTTP endpoint served" failed but L5.2 passed → check that `dsh plugin` reconciler activated the bundle (look for `# == <name>` in `dsh --dump-config`).

The new lesson (`dsh-plugin-l5-web-smoke-cordis-duplicate-entry`) is the canonical reference for the duplicate-id family; the older lesson (`dsh-plugin-install-github-codeload-timeout`) is the canonical reference for the install-timeout family.

---

## Second session (2026-09-03): intake question routing fix + #1396 reclassification

Local-only rolling details in `docs/prd/HANDOFF.md` §6 (file is gitignored).

- **Question → lesson misfire root-caused via #1396** (Portuguese `[Intake]`, auto-rejected 16.9/100 → badcase → salvage): the issue-level pipeline is lesson-shaped by default (`issue-intake-triage.yml` classifies everything non-test as `lesson-candidate`; `intake-auto-review.yml` scores every intake as a would-be lesson). Also found: triage raced `opened`+N×`labeled` → duplicate triage comments; type detection is EN-keyword regex that the Opire footer ("add"/"documentation") corrupts → same issue reported `unknown` yet got `type:feature`.
- **Fix shipped to remote main `0e6e4f3f`** (tree `3436d338`, content = local `1a86f24f`; git protocol down → Git Data API push):
  - `issue-intake-triage.yml`: structured `Kind:` wins → `kind=question` = `question` type (never feature/bug), adds `question`/`type:question`, removes stale `auto-rejected`/`needs-salvage`/`type:*`; drop `labeled` trigger (dup-comment race); strip `<details>` boilerplate before keyword detection.
  - `intake-auto-review.yml`: `kind=question` skips lesson scoring + badcase archive → `## [QUESTION]` routing comment + `question`/`type:question`/`needs-human-review` labels.
- **#1396 handled as a question** (user decision): body `Kind:` → `question`, title `[Intake]` → `[Question]`, labels reconciled by the new triage on the `edited` event (verified live), clarification comment posted (EN+PT).
- **Follow-ups (not done)**: entry-level question detection in `mcp_http_server.py`/worker (auto `kind=question` for how-to/no-fix intakes); `intake_pipeline.py` should stop minting lesson-shaped drafts for `kind=question`; #1397 (same Portuguese batch) still needs triage.
- ⚠️ Local `main` = `1a86f24f` vs remote `0e6e4f3f` (identical trees; API-pushed commits differ in hash). On network recovery: `git fetch origin main && git reset --mixed origin/main`. Do not force-push.

---

## Third session (2026-09-03): MCP intake completion — question routing at the entries

- **Goal**: the "question vs failure" fix from §2 only helped intakes that arrived with explicit `kind=question`; #1396 arrived as `missing_lesson` (the default every agent used) and still fell into the lesson funnel. This session closes the MCP entries so question-shaped content self-routes.
- **Shared detector** (multilingual EN/PT/ES/ZH, mirrored JS↔Python):
  - `workers/lib/utils.js` → `inferIntakeKind()` (+ `QUESTION_HINTS` / `FAILURE_HINTS` exports)
  - `scripts/intake_kind.py` → same tables/rule
  - Rule: explicit valid kinds honored; `missing_lesson` (explicit or default) is upgraded to `question` ONLY when there is clear question phrasing AND no structured failure fields (error/fix/verification) AND no inline error text (traceback/`Error:`/error codes — narrow on purpose so "how do I structure a failure lesson?" stays a question while pasted stacks stay failures).
- **`workers/register-proxy-sw.js`** (prod remote MCP): submit_intake resolves kind via detector, echoes `routing:{kind, auto_detected}` in the response; no-match search suggestion now points how-to queries at `kind="question"` (error-like queries stay `missing_lesson`); submit_intake tool description documents question routing + auto-detect.
- **`scripts/mcp_http_server.py`** (local HTTP MCP — the entry that created #1396): kind whitelist validation, `[Question]` title + `needs-human-review` label for question, auto-detect, routing echo, no-match suggestion split.
- **Tests**: `workers/intake-kind.test.mjs` +12 (auto-detect EN/PT/ZH, no-override for failures/non-missing kinds, routing echo), `workers/mcp-no-match.test.mjs` +2 (suggestion split), new `tests/test_intake_kind.py` (26 cases incl. parity edges). Worker suite 103 pass, related python subset 107 pass.
- **Docs synced**: README "Questions vs failures", docs/integrations/mcp-remote.md, docs/skill.md, skills/misakanet/SKILL.md, llms.txt.
- **Not done**: `intake_pipeline.py` still mints a lesson-shaped draft into D1 `lesson_drafts` for kind=question (it is a CI-only path today, not the prod MCP route); #1397 still needs triage. Detector is best-effort — content without question phrasing stays `missing_lesson` (e.g. the original #1396 PT sentence without "como" would still need human eyes).

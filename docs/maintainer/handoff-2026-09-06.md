# Handoff — 2026-09-06（DCO 体验修复轮，issue #1498）

> 合并：`8cc2967f3`（PR #1502，squash）→ main 现 HEAD `8cc2967f3`。
> 上游承接：`docs/maintainer/handoff-2026-09-05-audit-improvements.md`（Wave 3 及其挂起项）。
> 本会话主题：取证并修复贡献者抱怨的 DCO 体验问题（issue #1498）。

---

## 1. 会话概览

- **触发**：issue #1498（hummern，PR #1487 作者）：`needs-dco` 标签不自动清除，
  补签 force-push 后 bot 评论仍红，被迫推新提交触发重扫。
- **动作**：REST 取证（PR 时间线/check-runs/标签）+ 全仓代码定位根因 + 量化影响 →
  P0 backfill 清标签 → P1 workflow 修复（PR #1502）→ P2 文档 → 回复并关闭 issue。
- **成果**：102 个失实 `needs-dco` 标签清除（3 open 解锁 + 99 closed 卫生）；
  标签生命周期改为"每次 push 重扫、全签自动摘除"；3 个 open PR（#1400/#1411/#1461）
  不再被误判为"等作者补签"。

## 2. 根因（详见 docs/maintainer/dco-experience-issue-1498.md）

1. `needs-dco` 标签**只加不删**：`pr-shape-guard.yml` Gate 4 仅在未签时 add，
   全仓无任何 `removeLabel('needs-dco')`；`pr-quality-gate.yml` 反把残留标签当
   `ready-to-merge` 硬阻断 → 修好后永久红标。
2. 三套 DCO 实现语义分裂（dco-check 含 merge 提交 / dco-audit `--no-merges` /
   shape-guard 含 merge 提交）→ 误判源。
3. 失败评论只追加不清扫（REGULATORY BLOCK + 逐次 audit 报告）→ 界面自相矛盾。

## 3. 量化证据

- merged 415 中 64 (15.4%) 仍挂 `needs-dco`；open 3 个全部 0 未签名提交（100% 误挂）。
- #1487：13:26 labeled → 16:47 merged，全程无 unlabeled；最终 head 上 dco/audit/quality-labels 全绿。

## 4. 改动（PR #1502，merged `8cc2967f3`）

| 文件 | 改动 |
|---|---|
| `.github/workflows/pr-shape-guard.yml` | Gate 4：全签 → `toRemove needs-dco`（每次 push 生效）；merge 提交豁免 |
| `.github/workflows/pr-quality-gate.yml` | `ready-to-merge` 由 DCO check run 结论驱动；标签过清/败加双保险 |
| `.github/workflows/dco-check.yml` | `--no-merges` 对齐 |
| `.github/workflows/pr-checks.yml` | audit 报告 marker upsert（就地更新）；DCO 过清 REGULATORY BLOCK |
| `CONTRIBUTING.md` | DCO 自动清除说明 + Node.js Test Conventions（fixtures/、`this.skip()`） |
| `docs/label-system.md` | `needs-dco` 自动清除 |
| `scripts/dco_label_backfill.py` | 维护者标签回填工具（本次已用） |
| `docs/maintainer/dco-experience-issue-1498.md` | 取证+影响+方案+执行记录 |

## 5. 状态与验证

- backfill：`removed=102`（3 open + 99 closed）、`kept=200`（确有未签名提交，语义真实）。
- #1502 CI：DCO/audit/audit-shape/tests 矩阵/CodeQL 全绿（pr-agent 为非阻塞 review bot）。
- audit 报告 marker upsert 首跑实测生效（#1502 评论区单条就地更新）。
- issue #1498：已回复（致谢+根因+修复+backfill 数据）并 closed。

## 6. 运维备注

- **日常**：`python3 scripts/dco_label_backfill.py --dry-run` 可复查是否有失实标签
  （--dry-run 只报告不删）。
- **行为变更提示**：merge 提交不再要求 sign-off（三处检查已统一）——对"git merge main"
  的贡献者是减负；对旧 PR 语义放宽，已在文档注明。
- 上一会话挂起项不变：audit 红门 PR 队列、DSH 上游提案、MCPVault 认领、
  registry 发布（mcp-publisher）、wrangler KV 占位符等（见 handoff-2026-09-05-audit-improvements §Wave 3）。

## 7. 回滚

- 整体：`git revert 8cc2967f3`。标签自动摘除逻辑在 shape-guard/quality-gate 两处，
  若需回退旧行为需同时 revert 该提交（含 dco-check --no-merges 与 pr-checks upsert）。

---

# Wave 2 更新（同日，bot 文案一致性 + #1430 收尾）

## 评估结论：md 之外的"机器人回复文案"此前未更新 → 已补

初轮修复只覆盖了标签状态机/检查语义/文档（md）。审计所有 bot 对外文案后发现
**仍与旧行为矛盾**，已随 **PR #1503**（merged `34041298b`）修复：
- `pr-welcome.yml`：对**所有**首 PR 无条件发 "Fix DCO First"（即使已全签，如 #1487/#1430），
  结尾还写 "CI runs automatically once DCO passes" → 改为**按 commits 实际状态条件展示**，
  通过则显示 "DCO: all commits signed ✅"；措辞承诺自动清除、勿推空提交。
- `dco-check.yml`：检查输出补修复命令 + merge 豁免 + 自动清除说明。
- `.github/actions/dco-audit/action.yml`：废弃惊悚的 "REGULATORY BLOCK / AUTO-MERGE: PINNED"
  文案 → 友好修复指引（含 `/fix-dco` 提示 + 自动清除说明）；评论改 **marker upsert 不堆叠**。
- `pr-checks.yml`：改用**本地** action（外部 `Ikalus1988/dco-audit@v1` 已 2 个月未更新，
  且本地副本曾是死代码——单一事实源问题一并解决）；清理同时匹配新 marker 与旧文案。

## audit 管线健壮性（#1430 排查中发现的真问题，随 #1503 修复）

- **Dependency Audit 在 workflow_dispatch 下必失败**：无 PR base/head SHA → `git diff` 报错；
  且 always() 报告因此对 docs-only PR 打出误导性 "❌ Test suite failed"（实测复现）。
  → dispatch 时跳过 dep audit；Test Suite 报告/硬门禁把空 OUTCOME（manual 跳过）当 "未失败"。

## PR #1430 卡点诊断与处置（docs: MCP intake 引导，#1071）

- **卡点**：内容早已就绪（09-02 维护者要求的格式修复 + 09-03 rebase 均完成），
  但 `audit` 检查红 —— 拉日志定位：`tests/test_intake_kind.py` 2 例因
  `ModuleNotFoundError: mcp.server.fastmcp` 失败 = **mcp 2.x 环境漂移**（旧
  `mcp_http_server.py` 用 v1 导入），与 PR 内容无关；上游 #1478（mcp SDK v2 迁移）已修，
  但 #1430 的 merge ref 停在 09-03 main（#1478 之前）→ 陈旧红标。
- **处置**：删掉误发的手动 audit 报告评论 → 说明根因并 **squash 合并（`cab150ff3`）**。
- **教训**：fork docs PR 若长时间未 rebase，其 CI 反映的是旧 base 状态——维护者应按
  "merge ref 是否已过期"判读 audit 红，而非等作者；另全量测试对 docs-only PR 仍会跑
  （scope 判定不含 docs-only 分支），是 audit 噪音来源之一（后续可优化 scope 规则）。

## 状态速览

- main HEAD `34041298b`（含 #1430 + #1503）。PR #1503 已合并。
- 上一节挂起项不变；新增待办：可复查 `scripts/dco_label_backfill.py --dry-run`
  确认无新失实标签。

---

# Wave 3 更新（同日，zsxh 反馈批量处置：#1400 CI 不触发 / audit / gate）

zsxh 反馈三类"无法修复（需维护者）"问题，逐一定位并处置：

## 1. #1400 CI 不触发（cross-platform 未跑）——根因 = merge 冲突，非 repo 配置
- 证据：`merge_commit_sha=None`、`mergeable=False`（dirty）；head `61e06d26` 的
  **pull_request 工作流 run 数为 0**，只有 pull_request_target（shape guard/welcome）在跑。
  → GitHub 无法为冲突 PR 生成 merge ref，pull_request 事件工作流不启动。
- 附带：该分支 diff 是巨型噪音（删 codeql.yml/deploy-worker.yml、data/lessons.json −6567、
  docs/mcp-intake-guide.md −223 等 = 旧 main 误合并/revert 残留），无法干净合并。
- 处置（作者侧，维护者可代推）：从当前 main 重建，仅保留 e2e 测试增量。

## 2. audit 红（#1494/#1493/#1461/#1452/#1451/#1437/#1399）——主仓库已修，多数为陈旧 run
- **mcp v2**：主仓库已由 #1478（09-05 03:51 合并）修复；本地全仓已无 `fastmcp` 导入。
- **版本漂移**：#1494/#1493 的 audit 红（09-05 16:21）= `test_version_consistency.py::test_pypi_line_lockstep`
  瞬时失败（pyproject 2.28.0 vs server.json pypi 2.27.1），#1492（registry/pypi 对齐 2.28.0）已修复；
  本地验证 test 通过。
- **实证**：close/reopen 触发新 run 后 #1399、#1437 的 **audit 均 ✅**；#1494/#1493 的 reopen
  被 GitHub 按 head sha 去重（只触发 pull_request_target，未触发 pull_request），下个 push 即绿。

## 3. gate 失败（lesson 重复标题/近似）——真实内容信号
- **#1452（WebCrypto, #1398）/ #1451（Vitest, #1384）= 重复提交**：lesson 已于 08-30/09-01
  由 zsxh 本人合入 main（90585ef30/96981af92，同一 issue）；gate 报 duplicate title +
  100% similar → **已按重复关闭**。
- **#1437/#1399（provenance 批量）**：#1399 报 256× missing evidence_level + 少量
  duplicate title/near-dup——批量补 provenance 元数据时未给部分旧 lesson 补 evidence_level；
  需作者补齐 frontmatter 或排除镜像。
- **#1461**：audit 已绿；shape-risk = 标题 docs: 但含 scripts 改动（范围越权）→ 改标题/拆 PR。

## 4. 待办
- #1411（Provenance batch 2）当前**全绿**（audit/gate/dco/tests 全 success），仅剩
  risk:high/generated-file human review → 优先审。
- #1494/#1493：作者下个 push 后 audit 应绿（gate/dco/tests 已绿）→ 可合（lessons 需人工）。
- #1400：等作者重建分支（或按需用 zsxh PAT 代推重建）。

---
*Wave 3 附记于 2026-09-06。*

---

# Wave 4 更新（同日，zsxh PR 批量合并轮）

zsxh 完成四 PR rebase/瘦身后（详见其上一条汇总表），维护者侧逐项处置：

## 已合并（3）
| PR | 说明 | merge |
|---|---|---|
| #1437 | 56 devops lessons provenance + evidence_level: E1（纯 frontmatter）| `24f3c062e4` |
| #1461 | MCP 远端端点评估报告（docs/mcp-eval-20260903.md）+ `import sys` 修复；docs-only 标签移除、重命名 docs+fix: 后 shape-guard 复跑 shape-safe | `be252c78fb` |
| #1411 | **299 lessons provenance batch + arch-review 三件套**（瘦身 +518/−75，DCO 补签后标签自动清除——#1498 流程实证生效）| `22842a48e6` |

## 已关闭（3）
- **#1452/#1451**：与 zsxh 本人已合入 main 的 lesson（#1398/#1384，commit 96981af92/90585ef30）100% 重复 → duplicate closed。
- **#1399**：与 #1411 299 个 lesson **完全重叠**（#1411 多 arch-review 且已修 evidence_level）→ superseded by #1411 closed。

## 关键发现与决策
1. **#1400 与 #1411 曾高度重叠**（lesson-security.yml +29/−24 逐字节相同、data/*.json 大 churn 同源）——#1411 瘦身后已分离；#1400 仍含 **lesson_gate.py 策略放宽**（`provenance.evidence` 可替代 `evidence_level` + 跨目录重复标题豁免）与 lesson-security.yml 重写 → **语义存疑**（provenance.evidence 是"入库溯源"，非 E0-E4 证据等级），**未合并，待拍板**。
2. **#1411 gate 256× missing evidence_level = 预存 legacy 债务**（main 上本就缺，gate 只校验被触碰文件才暴露）。已建 **issue #1506** 跟踪：legacy evidence_level 补齐 + gate"新增 vs 元数据触碰"差异化校验 + en/ 镜像 dup 误报。
3. **quality-labels 403**（fork PR 偶发）：pr-quality-gate `addLabels` 无 try/catch 的既有噪音，非本会话引入；可后续补防护。
4. **gate 校验范围**：lesson-gate 按"PR 触碰文件"全量新标准校验 → provenance 类元数据批量 PR 必然撞历史债。长期修复 = 差异化校验（见 #1506）。

## 待办
- **#1400**：需拍板 ——（a）拆出 e2e 测试单独合、（b）接受其 gate/security 策略变更（建议否，语义存疑）、（c）暂缓。
- #1494/#1493：audit 陈旧红（09-05 版本漂移，已修）→ 下个 push 即绿；lessons 需人工合并。
- 网络：github.com git 协议 16:40 起间歇不可达（REST 正常）；本 Wave 附记待网络恢复后推送。

---
*Wave 4 附记于 2026-09-06。*

# Wave 4b（同日收尾）

- **#1400 合并**（`2574167a17`）：zsxh 二次瘦身后仅剩 `tests/e2e_mcp_pipeline.py`（+180 独立 E2E，覆盖 #1359-1361）——policy 改动全部拆出，全绿合并。
- **#1505**（zsxh 新开 "discuss: gate evidence_level relaxation + security scan rewrite"）：策略内容独立成 PR，已发维护者评审——
  - fanuc tags −2：接受
  - lesson-security.yml 重写：接受（建议补 `~~~` fenced 支持）
  - lesson_gate.py：**evidence_level ⇄ provenance.evidence 等价建议否决**（语义不同维；新 lesson 不能无 E 级过门禁）；跨目录 dup 豁免建议收窄为 en/ 或同 stem 镜像 → 指向 #1506 差异化校验
- 本地 handoff 已 rebase 推送 `ac9714152`。

---
*Wave 4b 附记于 2026-09-06。*

# Wave 5（同日收尾：zsxh 反馈全部清零）

zsxh 三轮反馈（#1400 CI 不触发 / audit / gate / quality-labels 403）至此全部处置完毕，主仓库侧基础设施修复 3 项 + 贡献 PR 合并 6 个：

| PR | 内容 | merge |
|---|---|---|
| #1400 | e2e MCP pipeline 测试（瘦身纯测试）| `2574167a17` |
| #1411 | 299 lessons provenance + arch-review（#1399 superseded）| `22842a48e6` |
| #1461 | MCP 评估报告 + import 修复 | `be252c78fb` |
| #1437 | 56 lessons provenance + evidence_level | `24f3c062e4` |
| **#1507** | lesson_gate 接受 provenance.evidence（zsxh 拆出的 gate 修复）| `edd44ae7a6` |
| **#1508** | quality-gate label 写入 best-effort（fork 403 修复，回应 zsxh CI 配置问题）| `179c368a90` |
| #1505 | fanuc tags + security 扫描重写 + near-dup error→warning | `4debc9b9fd` |

**策略修正记录**：此前评审曾建议否决"provenance.evidence 顶替 evidence_level"；基于语料现实修正——main 上 336/360 lessons 无 E 级（E0-E4 从未回填），#1411 已确立 provenance.evidence 为 legacy 证据机制 → 等价接受（#1507）。新 lesson 仍要求 E 级 + dup 豁免收窄等严格化保留在 **#1506**。
**遗留**：#1506（legacy evidence_level 差异化校验/en 镜像 dup）、#1494/#1493（audit 陈旧红，下个 push 即绿）、handoff 本文档随会话推进已多次追加（Wave 2-5）。

---
*Wave 5 附记于 2026-09-06。*

# Wave 6（#1506 实施 + quality-gate 回归热修）

zsxh 反馈闭环后，继续实施 #1506（lesson 质量门差异化）——**PR #1509 merged `2d2030e132`**：
- `lesson_gate.py`：新增 `--existing` 模式 —— base 上已存在的文件（元数据/provenance 批量触碰）为**咨询性**（结构性发现降为 warning），**新增文件保持严格硬门禁**（新 lesson 质量保护不变）
- `find_duplicate_title`：跳过**同 stem 镜像**（core/x.md ↔ en/x.md），替代 #1507 的过宽跨目录跳过 → 跨目录非同 stem 重复恢复检测（带回归测试）
- `lesson-gate.yml`：`git diff --diff-filter=A/MR` 分流新增/修改，两条 gate 各自运行；gate job 内 unit tests 恒跑
- tests +6（50 passed）；issue **#1506 closed as delivered**（legacy E0-E4 回填降级为非阻塞债务）

**⚠️ 过程中自曝回归并热修**：#1508（quality-labels try/catch）编辑误删 `uniqueAdd/uniqueRemove` 两行 const → `ReferenceError` 使所有 quality-labels run 变红 → **#1510 merged `6877be6459`** 恢复声明。教训：workflow 里 github-script 块改动后应跑 node --check（本会话已补该习惯）——**#1508 合并时其自身 quality-labels 未触发，回归未被 CI 捕获**；建议后续给 pr-quality-gate 加本地 JS 语法自检步骤或测试。

**网络**：github.com git 协议 21:20 起再断（REST 正常）；Wave 6 附记本地提交待推。

---
*Wave 6 附记于 2026-09-06。*

# Wave 7（MCP registry / mcptoplist 发布打通——上个会话挂起项 #1 完成）

**目标**：server.json（2.28.x）发布到官方 MCP registry，mcptoplist 镜像同步。此前因沙箱 OAuth 出口不可达挂起，本次由用户在**本机**执行 login/publish 打通。

**解锁链条**（registry 逐层校验暴露的 3 个前置问题）：
1. **description ≤100**：registry 422 —— 用户本机 server.json 是旧版（163 字符）；pull 后 88 字符通过
2. **PyPI 版本缺失**：registry 400 "version 2.28.0 not found" —— 根因：`environment: pypi`（Trusted Publishing）从未在仓库配置 → release-pypi.yml 从未成功 → PyPI 停在 2.18.0（handoff 挂起 #3 的深层原因）。修复：API 建 `pypi` environment + 用户 PyPI 侧添加 Trusted Publisher（owner Ikalus1988/repo MisakaNet/workflow release-pypi.yml/env pypi）+ release-pypi 曾被禁用（enable API）+ 加 `workflow_dispatch`（**#1511**）→ dispatch 发布 **misakanet 2.28.0** 成功
3. **README 所有权标记**：registry 400 要求 PyPI README 含 `mcp-name: io.github.Ikalus1988/misakanet` → README 加标记 + 全通道 bump **2.28.1**（align_versions --source/--registry，**#1512**）→ dispatch 发布 2.28.1 成功（含标记验证）

**终态**：`mcp-publisher publish server.json` → **✓ Successfully published, version 2.28.1**；registry 权威确认；mcptoplist 镜像自动同步（有延迟）。docs/maintenance.md §8-10 已更新为当前状态。

**运维沉淀**：PyPI 发布现在走 `release-pypi` workflow_dispatch（version input）；registry 发布仍需本机 `mcp-publisher login+publish`（凭证 ~/.config/mcp-publisher，沙箱不可写）。版本通道现全部 2.28.1（registry/repo-release/pypi 三线合一）。

---
*Wave 7 附记于 2026-09-06。*

# Wave 8（intake-bot 方向：批准 → MVP → 拆 issue）

- 用户批准"爬虫可复用 intake bot"方向（设计见 docs/agents/crawler-intake-bot.md），并要求**最小实现 + 并行评审、交 zsxh 试点**、确认后再拆小 issue 分发（不都自己实现）。
- **MVP**：`scripts/intake_bot.py`（零依赖 CLI，merged `255cb557c`）：错误签名提取 → 预查（D1 全量语料+本地打分，停用词过滤）→ 指纹去重 → 质量门槛 → 三态（hit 建议/intake 候选(dry-run)/ignore）；`--auto-intake` 才匿名提交；四态 demo 已验。
- **评审发现服务端 bug**：`/api/lessons?search=` 过滤器未生效（任何 query 返回固定默认集，多参数实测）→ 已拆 **#1524**；MVP 绕道全量语料本地打分（与 search_knowledge --remote 同源）。
- **拆 issue（5）**：#1524 服务端 search 过滤修复（pool:quick）；#1525 intake-bot Action 封装（L1，zsxh 试点，pool:quick）；#1526 intake 服务端 canonical 去重；#1527 error-signature/failure_patterns 索引（提 0.489 命中率，pool:deep，分两阶段）；#1528 转换回执（intake→lesson 通知 source）。
- **待办**：等 zsxh 试点反馈（#1525）；#1524 建议优先（预查轻量化前置）；用户后续可把 #1526/#1527/#1528 标 bounty/claim 分配实现者。

---
*Wave 8 附记于 2026-09-07。*

# Wave 9（2026-09-07 同步：intake-bot 方向闭环 + 队列清扫）

## intake-bot 方向（上轮批准）
- **#1524 → #1534 merged `539365c087`**（zsxh 实现）：`/api/lessons?search=` 过滤器修复——register-proxy.js 加 `filterLessons`（title/summary/domain/tags AND 过滤 + limit）；register-proxy-sw.js 接受 `?q=`/`?search=`，D1 未绑定时客户端降级。单测 +61，全绿。#1524 closed。
- **#1525 → #1529 merged `89770da484`**（zsxh 实现，principal 合）：intake-bot 封装为 `.github/actions/misaka-intake-bot/`（action.yml 196 行 + intake-bot-demo.yml + `tests/test_intake_bot_50.py` 90% 准确率/50 样例）。#1525 closed。
- **MVP** `scripts/intake_bot.py`（main `255cb557c`）仍在，Action 封装其上。
- 待办：#1526（intake 服务端去重）/ #1527（error-signature 索引，pool:deep）/ #1528（转换回执）未动——可标 bounty/claim 分配。

## 队列清扫（平行会话 principal 已做部分）
- **#1493/#1494 内容已由平行 rebase-PR 合入 main**：#1494→#1531（pip-install-proxy-timeout.md, f4386e4c5）；#1493→#1532/#1533（corporate-proxy-curl-timeout.md）。两开放 PR 残留分支已按 delivered **closed**（本会话处置）。
- 本会话此前合并：#1400/1411/1437/1461/1505/1507/1508/1509/1510/1511/1512/1502/1503（zsxh/自建）。
- 其他合并（principal）：#1522（knowaguy4u-cell auto-fix-1221）、#1529/1531/1532/1533。

## 仍开放 PR（待 triage/后续轮）
- #1514（knowaguy4u-cell，[Bounty] dsh plugin 测试套件）——#1403 已由 #1487 交付，疑似重复，待核。
- #1513（kveita，Smithery badge 修复）、#1443（ghzhost，evidence_refs，作者 09-07 活跃）、
  #1412/#1413/#1415（Mr-Neutr0n，intake/Windows CI）、dependabot #1515/#1516/#1517/#1521。
- 状态：zsxh 试点反馈待收（#1525 邀请）；#1526-1528 待分配。

---
*Wave 9 附记于 2026-09-07。*

# Wave 9b（2026-09-07 收尾动作）

- **#1514 closed（duplicate）**：`[Bounty] dsh plugin test suite`（knowaguy4u-cell 自动生成，29 行 test_suite.sh + needs-dco）——#1403 赏金已由 #1487 交付，单赢家政策关闭。
- **#1526/#1527/#1528 标 `bounty`** + scope 评论：#1526 建议 zsxh（intake 服务端 canonical 去重，先以 intake_bot 预查/lesson_index 作参考）；#1527 design-first（error-signature 索引，Phase A 可独立）；#1528 与 #1526 解耦（source 台账 → 转正回执，复用 cite-lesson/notify）。
- 状态：intake-bot 方向闭环完成（#1524/#1525 delivered，MVP+Action 在 main）；试点反馈与 bounty 认领待进行。

---
*Wave 9b 附记于 2026-09-07。*

# Wave 9c（2026-09-07 issue 队列清理：已交付但未关的重要项）

按"哪些重要 issue 其实已自行完成"盘点后关闭 9 个：
- **bounty/lesson 交付**：#1403（dsh 测试套件，已由 #1487 交付）、#1368（pip proxy 课→#1531）、
  #1384（Vitest 课）、#1398（WebCrypto 课）、#1377（GitGuardian 占位课）、#1381（DCO sign-off 课）、
  #1221（社媒帖→#1522）
- **清理**：#1407（SMOKE 占位）、#1530（垃圾 intake YOUR PROBLEM）

**仍真正开放（重要）**：#1526/#1527/#1528（bounty 待认领）；#1222/#1223（Mr-Neutr0n PR #1412/#1413 未合）；
#1418（dsh-plugin-hub npm 登记）；#1504（priority:high intake，待核是否已 lesson 化）；#1459/#1396/#1397（question 待答）；
#818（社媒 bounty 无交付）；#1523（支付证明 question，待人工答）；intake 待 AC 小项（#1374/#1375/#1376/#1130/#1145/#1146/#1166/#1342…）。

---
*Wave 9c 附记于 2026-09-07。*

# Wave 9d（2026-09-07 纠错：#1221 误关已纠正）

- **纠错**：此前 Wave 9c 把 #1221（社媒帖 bounty）按 #1522 merged 关闭——**不成立**。复核 #1221 AC：需 maintainer review + 发布 ≥1 平台 + 指标；#1522 是自动生成器提交（模板化文案 + **错误链接** github.com/misakanet/mcp-remote（不存在；真实为 Ikalus1988/MisakaNet + misakanet.org/mcp）），未 review/未发布。**#1221 已重新打开**；#1522 留言标注交付不成立（视为草稿产物，非交付）。
- **教训**：关闭"delivered"issue 须**对照 AC 核实交付物内容**（尤其内容/社媒/文档类 + 自动生成器提交），不能只看 PR 标题声称 resolves + merged；链接类内容做存在性校验。其余 8 个已关项（lesson/bounty 代码交付，交付物在 main 可验证）无误。
- 待办：#1221 需真实交付（复用 #1197/#1198 review 过的草稿或重写 → 人工审 → 发布）。

---
*Wave 9d 附记于 2026-09-07。*

# Wave 9e（2026-09-07 社媒任务关闭）

- **#1221 / #818 closed**（维护者判定）：社媒发布对 agent 不可行（真人账号/反爬/风控/内容 judgment/发布/指标均为人在环）。#1197/#1198/#1522 已证 agent 路线不通。推广改走人工营销渠道（promotional/ + 真人发布），不再开放 agent bounty/competition。
- 教训：任务设计须匹配执行者能力——"人在环"任务（发布/审核/支付类）不应开放为 agent 竞争；agent 可交付物需机器可验收。
- 待推送：Wave 9c/9d/9e 三笔 handoff 提交（网络中断期间本地积压，恢复后补推）。

---
*Wave 9e 附记于 2026-09-07。*

# Wave 9f（2026-09-07 剩余开放 PR triage）

- **dependabot ×4 merged**：#1515（wrangler 4.129）/ #1516（create-issue-from-file v6）/ #1517（mkdocs-git-committers）/ #1521（setup-python v7）。
- **#1443 closed（superseded）**：ghzhost evidence_refs 校验（#1439）——功能已由 #1456 + ada9c08d5 合入 main（lesson_gate/schema/docs/tests 均在），迟交重复。
- **#1513（kveita Smithery 动态 badge）**：内容合理（workflow+脚本+README 1 行），review approve-after-DCO——已留言请补签。
- **#1412/#1413/#1415（Mr-Neutr0n）**：真实红（test×1 fail / lint fail）+ audit 噪音；分支落后 main（09-03 后 mcp v2/quality-gate/lesson-gate 大改）。对应 intake #1222/#1223/#1196 的 lesson 已在 main——已留言请 rebase+修复；代码/测试补强部分 rebase 后可合。作者 09-03 后未动，若再无响应按 stale 处理。
- 开放 PR 队列现状：#1513 + Mr-Neutr0n ×3（等作者）+ 新到（如有）。issue 队列：#1526-1528 bounty 待认领；intake 待 AC backlog 不变。

---
*Wave 9f 附记于 2026-09-07。*

# Wave 9g（2026-09-07 首次贡献者体验机制化）

- **触发**：#1513（kveita 首 PR）实例——fork 首次贡献者同时撞上"GitHub 一次性工作流批准门"（10+ runs 卡 action_required，仅 pull_request_target 的 audit-shape 在跑）与 needs-dco（真实未签 2/3）。
- **判定**：#1513 非 DCO 摩擦复现（标签语义正确）；残留摩擦 = 批准门无解释。
- **落地（#1537 merged `99abe60ec4`，关 #1536）**：
  - `pr-welcome.yml`（仅首次贡献者触发）：新增 "⏳ First PR: CI waits for one-time maintainer approval" 段——灰/等待=预期、维护者通常 1 天内批准、DCO 先修好（补签→标签自动清除）；
  - `CONTRIBUTING.md` Common Scenarios：补充 first-PR 灰检查解释。
- 备注：批准本身无稳定 REST 批量入口（approve 端点对已离开 action_required 的 run 返回 403）；维护者手工 approve 一次后后续 push 免批。
- 待验收：下一个真实首次贡献者 PR。

---
*Wave 9g 附记于 2026-09-07。*

# Wave 10（2026-09-07 intake-bot v1.0：补全 zsxh #1529 遗留）

- 用户指示"补全他的工作，完成本 PRD 需求 v1.0"。承接 #1529（Action 封装 merged 89770da484）+ zsxh 合并后两份自测报告（FP 38%@0.30；原 50 样例内容偏差）→ **v1.0 merged `c7644cc246`（PR #1539）**：
  - 默认阈值 0.30→0.45（CLI+action），泛化错误需 ≥0.55
  - 技术栈感知命中门（24 族词表）：跨语言不再因 module/not found/credentials 误配；裸 credential(s) 移出 git/aws
  - 噪音门：URL/JSON/路径/符号串→ignore；hit 带 suggest_only=true + 文案"仅供参考"
  - 测试重写为确定性离线套件（corpus 注入，30 passed，CI 含）；实测跨语言误配 19/50→0-1/9，同栈命中不受影响
- 教训（再次）：v1.0 编辑曾两次被 `git checkout -- .` 误还原 → 重建注意；handoff 提交尽量在 main 上直接做。
- 剩余：#1526/#1527/#1528 bounty 待认领（服务端去重/签名索引/回执，v1.1+ 范围）。

---
*Wave 10 附记于 2026-09-07。*

# Wave 11（2026-09-07 intake-bot v1.0 后续：dogfood + #1526）

分工：外部试点（3）→ 用户安排 zsxh；内部试点（1）+ 服务端去重（2，#1526）+ #1528 台账基础（顺带）由本会话完成。

- **#1540 merged `5e458fcff2`**：
  1. **内部 dogfood**：ci-lesson-search.yml 走 intake-bot v1.0 决策（stack-aware/noise/suggest-only），只在 hit 时评论（intake 候选/ignore 静默）——本仓库 CI 失败流量开始用 v1.0，验证手感。
  2. **#1526 服务端 canonical 去重背板**：submit_intake（kind=missing_lesson）开 issue 前查重——实词 ≥50% 命中单篇活跃课程 → `already_have{id,url,title}` 不开 issue；语料失败/不明确照常开（不吞新颖失败）。纯函数 findCoveringLesson + source 计数（KV，喂 #1528）。测试：intake-canonical 5 ✓、intake-kind 18 ✓、intake_bot pytest 30 ✓。
- **#1526 closed as delivered**（限流/信任模型另开）。
- 待办：#1528（转换回执，可用 source 计数接）、#1527（error-signature 索引）、外部试点反馈（zsxh）。

---
*Wave 11 附记于 2026-09-07。*

# Wave 12（2026-09-07 intake-bot 决策基准——外部试点的内部替代）

- 用户判定：zsxh 活跃度与外部爬虫流量不足，"外部试点"唯一候选是自身仓库 → 改为**内部离线基准**（可重复、可进 CI，优于一次性人工试点）。
- **#1543 merged `9b56b78fb`**：
  - `tests/benchmarks/intake_benchmark.json`（40 课 corpus 快照 + 44 hit/11 no-hit/8 ignore 样本；跨语言组取 #1529 FP 表；4 条真实形态）
  - `scripts/intake_benchmark.py`（precision/recall/noise/fp + `--check` 门禁；同域/同栈命中视为正确）
  - `scripts/gen_intake_benchmark.py`；CI `intake-benchmark.yml`（路径触发 + dispatch；修过一次：漏装 pytest）
  - `docs/benchmarks/intake-bot-v1.0.md`
- **基线：hit_precision 1.00（0 FP，对照 #1529 前 38%）/ hit_recall 1.00 / noise_ignore 1.00**（sim=0.45）
- **基准驱动引擎修复**：栈门放宽为"仅排除明确不同栈课程"——无栈通用课程不再被误滤（此前同课强词面仍 miss）；跨栈 FP 保护保留（30 单测仍过）。
- 后续：#1527（error-signature 检索）完成后可用本基准量化提升；`ci-lesson-search` 挂机观察真实流量作佐证。

---
*Wave 12 附记于 2026-09-07。*

---
*Wave 13 附记于 2026-09-07。*

# Wave 13（2026-09-07 failure_harvest P0——失败→lesson 批量转化的最小实现）

- 前置：用户批准 P0（评审通过后），并要求实现前核对开放 PR 是否已有打样。
- **PR 打样核对结论**：开放 PR（#1538/#1542/#1544/#1541/#1535 等）无 failure→lesson 管线；
  #1526 已于 #1540 合入（#1535 重复，应 triage 关）；#1538/#1542 是 #1527 error-signature 竞品
  （改 intake_bot.py，与 v1.0 会冲突，留意）；真正"打样"是已合入的 tombstone_to_draft.py 生命周期。
- **P0 分支 `feat/failure-harvest-p0`（`924c3384d`，已推未合）**：
  - `scripts/failure_harvest.py`：失败事件→指纹簇→lesson 骨架草稿（落 `lessons/drafts/`）。
  - 防过窄修复（demo 复现 8 簇全拆 → 3 簇 3 草稿）：
    1. `classify()` 取**最后/最内层**异常类名并剥模块路径（pip 3 措辞 → ReadTimeoutError 一簇）；
    2. 401/fatal/HTTP 码 = **弱信号**（进 failure_patterns），不作聚类键（git "401"+"fatal:" 两措辞一簇）；
    3. `norm_stack()` 检测前剥 URL（防 https→web 误报）+ 剔除 R 栈 `"r "` 单字符误报（"error " 尾 r 误命中）；
    4. 聚类键：specific kind → `K:<class>`（忽略栈差异）；generic → `G:generic|<栈>`。
  - 防噪音：occ≥min_occ(默认2) 才写草稿；what_tried 不豁免 occ=1；文件名跨平台安全。
  - `tests/test_failure_harvest.py` 10 用例锁定（40 pytest 全过）。
  - `lessons/drafts/README.md`：纳入 failure-harvest 第二来源 frontmatter 规范（harvest_ref/failure_patterns/E0）。
  - `docs/reviews/2026-09-07-intake-lesson-network-vs-ecosystem.md`：第一性原理评审记录（通过，有条件）。
- 待办：#1527 error-signature 索引（警惕 #1538/#1542 竞品 merge 冲突）、#1528 转换回执、
  failure_harvest 真实事件源接入（ci-lesson-search 失败事件 → harvest）、#1535 triage 关闭。

---
*Wave 14 附记于 2026-09-07。*

# Wave 14（2026-09-07 飞轮化：外部复用 action + zero-bounty 下发）

- 用户决策：若课程反馈 action 效果达标 → 开（0 赏金）issue 招募外部复用（含 zsxh），
  产 ≥50 样本报告 + 自动 lesson/question intake，启动"复用→issue→任务→lesson"飞轮。
- **PR #1549 merged `0c6c4c93d`**（飞轮前置 G1-G4）：
  - G1 `.github/actions/misaka-intake-bot` 自包含：无本地 checkout 时自动拉
    MisakaNet raw 的 intake_bot.py（纯 stdlib + 全远程），新增 `source-ref` input
  - G2 `sample_report.py` + `sample` output（NDJSON 样本行 + Job Summary）
  - G3 `docs/agents/external-usage.md`（1 行接入模板 + 参数 + 隐私）
  - G4 `docs/agents/dogfood-baseline-2026-09-07.md`：22 样本 → hit 9（0 误配）/
    intake 11 / ignore 2
  - 研究：`docs/agents/failure-feedback-flywheel-research.md`（飞轮机制 + 5 gap +
    赏金 AC 草稿 + 报告 schema）
- **Issue #1550（zero-bounty）已开**：外部真实仓库接入 misaka-intake-bot（suggest-
  and-intake）→ 交付 ≥50 样本报告（或 2-4 周累计）→ 产出自动 issue 清单/转正。
  标签：zero-bounty/ready/agent-friendly/help wanted/area:workflow/type:feature。
  动机：仓库可贡献 issue 不足且多为 intake——用外部流量产可贡献任务。
- 待办：通知 zsxh 等试点（用户在安排）；#1544（#1528 外部实现）合否待决策；
  #1538/#1513 等作者修复；#1527 error-signature 索引（#1538 竞品冲突留意）。

---
*Wave 15 附记于 2026-09-08。*

# Wave 15（2026-09-08 飞轮首轮试点回收 + 关键 bug 复现）

- 承接 Wave 14：#1550（zero-bounty，用户已改 ≥20 样本 + 加 bounty 标签）引来首批响应。
- **PR #1554（s6pa1rta3n-lab/roof4u 真实外部试点）**：25 样本报告（docs/external-pilots/
  roof4u-2026-09-08.md + .ndjson）。要点：
  - hit 10/10 on-target（git credential/pip proxy/permission/tfstate/postgres/token/JSON…）
  - noise 3/3 拦截、0 凭据泄露、去重 gate 生效
  - **带回 2 个上游真实修复**：action.yml Comment 步 env 缩进 bug（composite manifest
    解析失败）+ intake_bot.py `Origin: github.com` → 403（需改 misakanet.org）
  - 另附 3 条可转正 candidate 模式（playwright timeout/celery OOM/fastapi 422）
- **PR #1551（Silverbullets1 bot）**：#1550 的 bot 文本方案——非代码实现，待关（同 #1541/#1542 惯例）。
- **关键复现（本会话）**：main 当前 intake_bot.py `Origin: https://github.com` 提交
  misakanet.org/mcp → **403 Forbidden: invalid Origin**（外部 intake 全部被卡，飞轮阻塞）；
  改 `Origin: https://misakanet.org` 后提交成功（实测自动建 issue #1556，已关闭清理）。
  → **#1554 的 2 处修复为 P0 级，应尽快 review/合入**。
- 本会话清理：测试 issue #1556 已关（Origin 验证产物）。
- 待办清单见下节。

---

# 会话待办清单（2026-09-08 快照）

## 应立即处理（P0）
1. **Review/合入 #1554**（roof4u 试点报告 + action.yml env 修复 + Origin 修复）——
   解锁外部 intake（现 403 全卡）。合入前注意：PR 含对 action.yml/intake_bot.py 的改动，
   需核对与我们 main 版本无冲突；报告 AC 是否满足 #1550（25≥20 ✓）。
2. **关闭 #1551**（bot 纯文本方案，非实现）——沿用 #1541/#1542 惯例留解释评论。
3. 合入 #1554 后**回归验证**：本仓 external-usage 模板 + 一次真实 intake 冒烟。

## 开放 PR 队列（按优先级）
| PR | 内容 | 状态/动作 |
|---|---|---|
| #1554 | roof4u 试点 + 2 上游修复 | P0 review/合 |
| #1551 | #1550 bot 方案 | 关 |
| #1544 | #1528 intake 回执（s6pa1rta3n-lab，17 文件 CI 全绿） | 合否待用户决策（与自研 source-counter 路线二选一） |
| #1538 | #1527 error-signature（s6pa1rta3n-lab） | 等作者修 shape-guard（test 粘贴 markdown）+ CodeQL 高危 + DCO |
| #1513 | Smithery badge（kveita） | 等作者补签 DCO（Ikalus1988 已 approve-after-DCO） |
| #1412/#1413/#1415 | Mr-Neutr0n intake/Windows CI | 等作者 rebase（09-03 后未动，超期按 stale） |

## 开放 issue（可贡献面）
- #1550（zero-bounty+bounty，≥20 样本试点）——已有 roof4u 报告，可继续招更多试点
- #1527/#1528（bounty，与 #1538/#1544 对应）
- #1342/#1166/#1065/#1063/#869（ready/zero-bounty/intake 长尾）
- 大量 [Intake] backlog（用户观察：可贡献 issue 不足、多为 intake——飞轮转正后可缓解）

## 本地遗留
- `feat/ci-search-visible`（1 个未推提交 2348e69b3：ci-lesson-search v1.0.1 可见性增强）
  ——建议合入（使本仓 dogfood 决策可见/novel 提示），或与 #1554 一起评估
- untracked：scripts/mhs_watch.py + config（MHS 监控，另一任务）、docs/agents/mhs-watch.md、
  .archify-tool/、reports/ —— 均非本会话产物，勿误提交

## 后续（飞轮推进）
- #1554 合入后：用 roof4u 的 novel intake（pytest_mock/sqlite3/alembic/celery…）验证
  worker 建 issue → triage → 转正链路；3 条 candidate 模式可开 lesson 任务
- 转换回执（#1528）：#1544 若采用则闭环；否则自研 source-counter 续
- error-signature 索引（#1527）：等 #1538 修复或自研，提 0.489 命中率
- failure_harvest 接 ci-lesson-search 失败事件（真实源接入）

---

# Wave 16 附记于 2026-09-08（会话续：Wave 15 待办 P0 收尾 + 回归）

承接 Wave 15 待办清单（P0 三项）逐项处置完毕，另处理队列 2 项：

## P0 完成

1. **#1554 review + 合入（P0-1）→ squash `0a17ce6b5`**：
   - 审阅逐项核对（Wave 9d 教训：对照内容而非标题）：
     - `action.yml` `env:` 缩进修复：main 确有 bug（env 嵌套在 `with:` 内 → composite
       manifest 解析失败）；修复版 YAML 复验通过（env 在 step 层、`with:` 仅含 script）。
     - `intake_bot.py` Origin `https://github.com` → `https://misakanet.org`：main 上复现
       403（Wave 15 已证）；修复正确。
     - 报告 hit 行 10/10：引用的 9 个 lesson id 全部在 `lessons/contrib/` 实存；服务端
       佐证 intake #1552/#1553（source=roof4u）真实落库；25 样本 ≥ AC 20。
   - 流程：draft→ready（REST PATCH 无效，改用 GraphQL `markPullRequestReadyForReview`）
     → review APPROVE → squash 合并。CI 全绿（dco/audit/tests×9/CodeQL/build）。
   - **报告保真度跟进项**（已留评论，bounty 结算前请作者对齐，非合并阻断）：
     md §2 指纹/receipt 列与提交 NDJSON 不一致（95344ce13 重生成数据后 md 未同步）；
     §3 #1552 `test error` 已被服务端 auto-rejected，建议标注。
2. **#1551 closed（P0-2）**：`solution_1550.md` 纯文本方案（无真实接入/无样本报告），
   按 #1541/#1542 惯例留解释评论后关闭。
3. **回归验证（P0-3）**：合并后远端文件两处修复确认在位；真实 intake 冒烟成功
   （Origin misakanet.org → 无 403，issue #1557 建立即关，同 #1556 先例）。

## 队列处理

- **#1550**：随 #1554 合并（"Closes #1550"）自动 closed；已留 AC 核验总结评论；
  bounty 结算待 owner 真人侧（本会话不涉及支付）。
- **feat/ci-search-visible → PR #1558 merged `297e93db`**：ci-lesson-search v1.0.1
  可见性增强（决策进 run 日志 / hit 帮助式评论 / 同仓 novel-intake 提示 / fork 静默）；
  修正了 workflow 注释中错误引用的 "PR #1545"（该号实为 failure_harvest P0）；CI 全绿。
- #1544/#1538（s6pa1rta3n-lab）仍 draft：#1544 合否二选一待用户拍板；#1538 等作者修
  shape-guard/CodeQL/DCO。作者今日已交付 #1554，可借此 momentum 催 #1538。
- #1513（kveita）needs-dco 未清；Mr-Neutr0n ×3 超期未 rebase——均等作者。

## 本地遗留（勿误提交）

- untracked：scripts/mhs_watch.py + config、docs/agents/mhs-watch.md、.archify-tool/、
  reports/ —— 非本会话产物。本会话临时 helper `scripts/_gh.py` 用毕已删。

## 待用户拍板

- #1544（#1528 intake 回执实现）合否：采用外部 PR vs 自研 source-counter 路线二选一。
- #1550 bounty 结算（roof4u 交付已核验，含 2 项保真度跟进）。

---

# Wave 16b（同日：决策落地记录）

用户对 #1544（#1528 回执实现）拍板：**暂缓**。落地动作：
- **#1544 留言**（id 5587834183）：致谢后说明暂缓三条理由（需求侧证据不足/与
  #1540 KV 台账重复建设/PR 状态 dirty+巨型 churn）；将来首选自研最小版；PR 保持
  open/draft 不关闭，作者若继续须先去 churn + 台账对齐（不承诺合入）。
- **#1528 留言**（id 5587834537）：路线决策记录——保持 open（bounty/backlog），
  将来走自研最小版（复用 register-proxy.js KV source 统计，仅补"转正→回执"一环，
  含 evidence_level）；bounty 认领者须按最小范围出方案，勿复制 #1544 全套架构。
- 本会话踩坑记录：urllib 发 POST 会跟随 3xx 重定向转成 GET（返回列表而非评论对象）
  → 评论区操作统一走 curl。

## 剩余待用户拍板

- #1550 bounty 结算（roof4u 交付已核验，含 2 项保真度跟进；真人侧操作）。

---

# Wave 16c（同日：纠错——#1550 为 zero-bounty，无金钱结算）

- **纠错**：Wave 16/16b 两次把 #1550 记为"bounty 结算待用户拍板"——**不成立**。
  复核 #1550 正文《产出与回报》：**Zero-bounty：0 金额；merge credit + leaderboard
  积分**，标签说明同注（zero-bounty = 0 金额，仅 merge credit + leaderboard）。
  无金钱结算流程，此前措辞系误解（该 issue 带 bounty 标签仅为招揽 bot 可见性）。
- **收尾**：#1550 追加澄清评论（id 5588132875）：交付核验通过、0 金额、致谢
  s6pa1rta3n-lab 首个外部试点（merge credit + leaderboard）；2 项报告保真度跟进项
  标注为"作者有空对齐即可，不强制"。
- **教训（再次）**：判定交付回报前先读 issue 正文的报酬条款，勿凭标签（bounty 与
  zero-bounty 并存时以正文金额为准）。
- 至此 Wave 15 待办 P0 + Wave 16 队列处置全部收尾；无剩余待用户拍板项。

---

# Wave 16d（同日：#1550 误关纠正——长期开放征集，关闭条件 = 20 位贡献者）

- **纠正**：Wave 16c 把 #1550 当"已收尾"处理是**误判**（且 #1554 正文 "Closes #1550"
  已触发过一次自动关闭）。用户澄清：#1550 与 Remote MCP 上线前的多贡献者验证征集
  同模式——**累计 ≥20 位贡献者交付完整测试报告后才关闭，期间持续鼓励**。
- **动作**：
  1. #1550 **重开**（PATCH state:open），正文追加"⚠️ 长期开放征集"进度段：
     Pilot #1 = roof4u（25 样本，PR #1554）✅，还差 19 位；**防误关约定**——
     交付报告的 PR 不得写 "Closes #1550"。
  2. 更正评论（id 5588208271）：收回"无待办"误导表述，附 2 分钟接入指引与鼓励文案。
- **维护约定（写入 handoff 防再犯）**：#1550 的关闭由维护者按 20 位进度执行；
  未来任何合入的试点报告 PR 均不得 claim "Closes #1550"；进度更新维护在 issue 正文
  顶部进度段（每收 1 份 +1）。
- **持续鼓励渠道**（建议后续）：外部渠道（社媒/群）周期性转发 #1550 征集；
  本仓 dogfood/external-usage 文档可放 issue 链接；pr-welcome 等欢迎流暂不加钩子。

---

# Wave 16e（同日：置顶 issue 纠正 + Glama connector badge）

## 置顶/误关纠正
- **发现**：仓库 pinned issue 共 2 个，#1258（[Onboarding] 入口，body 首句 "always open"）
  被 register.yml **自动误关**（根因：#1258 标题含 "Register" → reopen 触发注册流程
  "注册完成→close"；2026-09-08 复现两次：reopen 后 13 秒被 github-actions[bot] 关闭）。
- **纠正**：
  1. #1258 重开并稳定（验证 20s+ 仍 open）；标题改名去触发词 →
     "[Onboarding] Connect your agent & submit failures via MCP"（立即免疫）。
  2. **register.yml 防御修复**（分支 fix/register-skip-onboarding）：job if 排除
     `startsWith(title,'[Onboarding]')` 类聚合入口 → PR（防未来 onboarding issue 再被误关）。
  3. **#1550 置顶**（GraphQL `pinIssue` mutation，REST 无 pin 端点）——与 #1258 同列
     pinned（2 个 pinned，均 OPEN）。
- **经验**：① GitHub GraphQL 有 `pinIssue`/`unpinIssue` mutation（此前误以为仅网页可操作）；
  ② "always open" 聚合 issue 的标题必须避开 register.yml 触发词（join/register/加入/注册/
  Registration/email-register），否则 reopen 即被注册流程关闭。

## README badge
- 新增 Glama **connector** badge（tool definition quality + endpoint health）：
  `https://glama.ai/mcp/connectors/org.misakanet/misaka-net/badges/score.svg`（验证 200），
  置于 README Ecosystem badge 组（保留原有 Glama servers badge）。commit 1bd43e4fe。

## 状态
- pinned：2（#1258 OPEN + #1550 OPEN）；#1550 进度 Pilot 1/20 继续征集。
- register.yml 修复 PR 待 CI 合入。

---

# Wave 16f（同日：KV 分析报告 → 11 个解耦 issue）

用户批准把 `reports/user_analysis_2026-09-07/`（CF KV + 邮件分析，162 keys + 10 封）
产出拆成小 issue 实现，**解耦原则：文件级/模块级隔离，实现互不阻塞**。

## 关键分析结论（开 issue 依据）
- 19 条 gap 中 **2 条伪盲区**（dco sign-off / rag latency——仓库已有 6+/13 篇 lesson，
  是 worker-search **召回缺陷**而非缺课）→ 先修召回，勿写重复课
- email-intake 5/5 intakeType=unknown（detectIntakeType 未命中）；
  rate:email 按"间隔"实现致跨周营销信放行（MCPVault 3 封实证）——两 bug 分处
  email-utils.mjs / index.js，文件级隔离
- traffic 只留 3 天、mcp_token 无回收、gap 无清理——三个 cron 维护任务 handler 独立
- 真盲区 lesson：Vertex 簇 / Agent 工程簇 / 运维簇

## 新开 issue（11）
| # | 类型 | 解耦边界 |
|---|---|---|
| #1562 | Bug 搜索召回 | 只碰 register-proxy-sw.js 搜索/索引；与 #1527 异源 |
| #1563 | Bug email 分类 | 只碰 email-utils.mjs（与 #1564 文件隔离）|
| #1564 | Bug email 限频 | 只碰 index.js rate 段（与 #1563 文件隔离）|
| #1565 | Ops traffic 月聚合 | 独立 cron handler |
| #1566 | Ops mcp_token 清理 | 独立 cron handler（#1565-67 可共享 worker 骨架）|
| #1567 | Feature gap→lesson 生命周期 | 周期扫描，与 lesson 任务事件解耦 |
| #1568 | Lesson Vertex 成本归因 | lessons/contrib 新 md |
| #1569 | Lesson Vertex Embedding | 同上（互不依赖）|
| #1570 | Lesson Vertex 工程 ×2 | 同上 |
| #1571 | Lesson Agent 工程 ×5 | 同上 |
| #1572 | Lesson 运维 ×2 | 同上（docker compose 若重复改提 #1562）|

lesson 类（#1568-72）走 lesson-gate 严格校验（E0-E4 + provenance）；交叉引用已回填。

## 遗留
- 敏感 gap（gemini safety bypass）未开 issue，需人工评估（合规 or 清理）
- 报告 §③ 方向性建议（节点健康/Dashboard/反馈闭环）未开 issue，属长期规划

---

# Wave 16g（同日：lesson 批次挂 bounty 认领 + 结算说明）

给 #1568–#1572（5 个 lesson 任务）挂 **bounty** 标签并在 issue 本体追加
"💰 贡献结算"段（对齐 ai-bounty-template / docs/prd/02-wallet-bounty.md 表述）：

- **无赏金**：荣誉激励（Hall of Fame / competition / core-bounty），无货币赏金
- **积分榜**：合并计入 leaderboard 积分（docs/reputation.md，lesson 数/活跃度加权）
- **贡献墙**：PR 声明 Agent-Type/Supported-by → Contributor Hall of Fame
- **认领**：/claim 8 小时独占窗口
- 提交要求：DCO + evidence_level E0-E4/provenance + lesson-gate

labels 终态：#1568-72 = [lesson, new-lesson, ready, area:lessons, agent-friendly,
zero-bounty, bounty]。抽查 #1568 渲染 OK。

## 待办提示
- #1562-1567（Bug/Ops/Feature）尚未标 bounty——Bug 类（#1562/1563/1564）建议走
  pool:quick 或自研，勿全挂 bounty（质量风险）；待用户指示。
- lesson 批的认领者（zsxh 或外部 agent）跟进。

---

# Wave 16h（2026-09-09：issue 拆发后 24h 认领风暴 + 批量审核合并）

上轮拆的 11 个 issue（#1562-1572）发出后数小时内**认领风暴**（agent 社区响应），
本会话完成系统审核：

## 审核关闭（10，内容不符/模板 bot）
- #1575-1579（Silverbullets1）：`solution_*.md` 仓库根文本方案 ×5 → 关（#1541/42/51 惯例）
- #1589/1590/1591/1593（tsunafire）：改 AGENTS/ARCHITECTURE.md 冒充 lesson 交付 ×4 → 关
- #1559（yhq411）：README +17 标注 Fix #1528 未实现 → 关（#1528 已定自研最小版）

## 合并（11）
| PR | 内容 | merge | 对应 issue |
|---|---|---|---|
| #1582 zsxh | detectIntakeType 扩展分类+测试 | cadaa65f | #1563 ✅closed |
| #1583 zsxh | email rate 间隔→计数窗口 | b84fa948 | #1564 ✅closed |
| #1584 zsxh | BM25 hyphen 复合词 tokenizer | aca5d586 | #1562 ⏳部分（rag 待实测）|
| #1585 zsxh | traffic 日→月聚合 cron | cde66308 | #1565 ✅closed |
| #1560 zsxh | CI failure 52 案例→3 lessons+报告 | 3ca66bb8 | — |
| #1573 s6pa1rta3n-lab | docker compose + iap numpy lessons | 99a228b0 | #1572 ✅closed |
| #1580 s6pa1rta3n-lab | Vertex 成本归因 lesson（E3，质量高）| 03e704e3 | #1568 ✅closed |
| #1581 s6pa1rta3n-lab | Vertex embedding 迁移 lesson | 3403df48 | #1569 ✅closed |
| #1587 s6pa1rta3n-lab | Vertex streaming + safety passthrough | 89f44054 | #1570 ✅closed |
| #1588 s6pa1rta3n-lab | Agent 工程 ×5 lessons | ae887d6a | #1571 ✅closed |
| #1592 tsunafire | duplicate of #1588 + dco/audit 红 → 关 | — | #1571 |

lesson 内容抽查：#1580（Vertex 成本）结构真实、provenance 可查、verification 完整——
外部 agent 交付质量超预期；lesson-gate 全绿佐证。

## 处理中 / 待办
- #1586（zsxh gap→lesson）：diff 重复含 #1585 traffic 段 → 已留言去重，等作者 rebase
- #1566（mcp_token 清理）：仍无 PR，pool:quick 待认领
- #1562：BM25 hyphen 修复已合（#1584），**rag latency 召回待实测**（勿关）
- Mr-Neutr0n #1412/#1413/#1415：作者 09-08/09-09 已自行 force-push 更新，mergeable
  clean——待内容复核后合（base 仍旧）
- #1513（kveita badge）：dco/audit 红，等作者；#1538（error-signature）：conflicting，
  等作者 rebase（可催，s6pa1rta3n-lab 今日活跃）
- #1544：暂缓（CONFLICTING，保持 draft）
- 踩坑：本会话大量 REST 扫描触发 core quota 5000 耗尽（重置 1h）——后续批量操作
  优先 GraphQL 或 search 配额

---

# Wave 16i（同日收尾：Mr-Neutr0n 三 PR 复核合并）

- #1412（fatal-guard runhandler-env 测试）/ #1413（register.js 去重 −59 + UTF-8
  环境修复 + smoke 测试）/ #1415（R-2000iC rag lesson + metadata）——作者 09-08/09
  自行 force-push 更新后 22/24 checks 全绿、mergeable clean，内容复核通过 →
  合并 `01bc023c` / `185f2fa9` / `4ee11252`；关联 intake #1222/#1223/#1196 自动关闭。
- #1513（kveita）：留言 rebase --signoff 一次解决 DCO+audit。
- #1538（error-signature）：留言催（作者今日 5 PR 全合，顺势修 #1538 解锁 #1527）。
- 剩余 open PR：#1513/#1538（等作者）、#1544（暂缓）、#1586（等 zsxh 去重）。
- 待办：**#1562 rag latency 召回实测**（#1584 只修 hyphen）；#1566 待认领。

---

# Wave 16j（2026-09-09：A 组执行 + CF errors 初查）

## A2 #1562 实测结论
`rag performance latency optimization` 召回缺口 = **词面鸿沟非索引 bug**：query 词
(performance/latency/optimization) 在 rag lesson 标题中 0-1 处重叠；worker 简化 BM25
无同义扩展（本地 engine 有 _SYNONYM_MAP + metadata 加权）。dco 场景已由 #1584 覆盖。
→ issue 转 **lesson keywords/aliases frontmatter 索引增强**待办（与 #1527 同思路），
勿写重复课。已评论。

## A3 #1566 scope 修正 → closed as not-needed（重要发现）
实现前核实：`mcp_token:*` 写入均带 `expirationTtl: 86400*30`（L713-718）→ **KV TTL
已自动回收过期 token**（读时过期返回 null），cron 清扫无对象可做；email 型 node 无
TTL 但属永久成员设计。原 AC（报告 §③ "无回收" 判断）**基于对 KV TTL 的误解**。
真实缺口 = 测试注册污染（数据质量），另属注册白名单议题。已评论 + 关闭（不写空转
cron——审核价值：阻止了无效实现）。

## #1586
zsxh 仍未 push 去重（head 未变，mergeable dirty）——继续等。

## CF Workers Errors 初查（misakanet-register-proxy 225 / email-register 3）
- 本机 CF OAuth token 过期且 refresh 需 client 凭证（agently 加密配置，不可得）；
  wrangler 无登录态 → **无 CF 日志读权限**。
- 代码级检查：search query required 校验（L775）、BM25 fallback、keepalive
  Promise.allSettled——异常防护良好；225 大数最可能 = **fetch 网络类**（CF→
  github.com/raw 间歇失败，本机 git 协议同日也不稳）或特定输入路径 bug。
- **待用户**：dashboard Logs 页复制 1-2 条 exception message + timeframe
  （24h/7d），即可精确定位。

---

# Wave 16k（2026-09-10：CF 225 errors 根因定位 + keepalive 降噪修复闭环）

## 根因（dashboard Errors 样本确认）
misakanet-register-proxy 的 ~225 errors = **keepalive 自监控 cron（*/5）对
misakanet.org 公网回环探测（/api/health|counter|lessons）间歇 HTTP 522**，每次失败
console.error + throw → 错误刷屏。**非服务故障**：同窗口 MCP 流量正常（info 日志）、
手动探测 200（/api/counter 8.9s 偏慢是次要因素）。225 ≈ 每 5 分钟 × 高失败率，对得上。

## 修复（PR #1598 merged `34505cb9`，deploy #109 成功）
1. `runKeepaliveSweep` 失败**降噪**：KV 连续失败计数，单次/瞬时 → warn；
   **连续 ≥3 次才 error + throw**；健康 sweep 重置（scheduled 传 env + export）
2. wrangler.toml `[triggers] crons = ["*/15 * * * *"]`（探测噪音降 1/3）
3. 测试 workers/keepalive-debounce.test.mjs（单次 warn / 3 次 escalate / 成功重置）
   ——本地 7 pass（含 traffic-aggregation 回归）

## 排查链路沉淀（教训：先查知识库！）
- 用户提醒"用 mcporter 授权"——此前已有 lesson
  mcporter-cloudflare-oauth-endpoint-scope-wsl-callback（本会话开头没先查，走了
  wrangler OAuth + dash.cloudflare.com 端点的弯路，两次 WSL 回调失败）
- 新发现并沉淀 lesson（870122a70）：
  cloudflare-observability-mcp-oauth-separate-domain——**每个 CF MCP 产品独立 OAuth
  域**（observability.mcp.cloudflare.com 自带 authorize/token/register），token
  audience 绑定不可互换；mcporter client 陷阱；state.txt 需 JSON 字面量；WAF 1010
  需浏览器 UA；WSL 手动 code 交换；直接 MCP 协议调用（initialize/tools/call）
- observability 已启用（用户 dashboard 操作）→ 错误明细今后可 MCP 直查

## 待观察
- 下一个 cron 周期起错误计数应停止增长（522 若再现仅 warn，连续 3 次才 error）
- CF home Analytics 的 Errors 历史 225 不会自动清零（历史计数），看"增量"

---

# Wave 16l（2026-09-10：CF OAuth 授权复盘 + 一键工具化，杜绝再折腾）

## 复盘：为什么这次授权折腾 5 次才成功
1. **会话开头没先查知识库**——已有 lesson（mcporter-cloudflare-oauth-...）直到
   用户提醒才读（用户原话："为什么这个技能给忘了""不要老犯同样错误"）。违反
   CLAUDE.md/AGENTS.md 第一规则（search first）。
2. **知识有盲区**：旧 lesson 只覆盖 mcp.cloudflare.com 域；observability 是**独立
   OAuth 域**（authorize/token/register 全在 observability.mcp.cloudflare.com），
   token audience 绑定、不可跨域互换——旧 lesson 没写，导致"授权成功但 token 无效"
   的循环。
3. **手工多步易错**：wrangler OAuth（dash 域 + WSL 回调）×2 → mcporter auth
   （client_id 无效）→ 错域手动 URL → 复用旧 client（域错）→ 才到正确路径。每一步
   都是新坑：state.txt JSON 字面量、WAF 1010 需浏览器 UA、redirect_uri 一致性。

## 根治：一键脚本 scripts/cf_mcp_auth.py（已提交）
- 所有教训内置：按 server 读 OAuth discovery（不假设域）→ 显式 DCR →
  无 scope → PKCE → 打印授权 URL（WSL 陷阱提示：跳转失败是预期的，粘贴地址栏
  URL）→ 自动提取 code + 校验 state → 浏览器 UA 换 token → 写 tokens.json →
  **MCP initialize 验证后才 exit 0**。
- 用法：`--server cloudflare-observability`（授权）/ `--refresh`（30 分钟过期后
  免浏览器续期）/ `--verify`（校验）/ `--reset`。
- 已验证：--verify（workers-observability 0.5.5 ACCEPTED）+ --refresh 均 exit 0。
- lesson（cloudflare-observability-mcp-oauth-separate-domain.md）已追加
  "one-shot tool" 段指向脚本。

## 仓库主人防再犯机制（本次落地）
- **维护 SOP**：凡 CF MCP/workers 操作，第一步 `python3 scripts/cf_mcp_auth.py
  --server <x> --verify`；缺失/过期就跑授权或 --refresh——永远不再手工构造 OAuth。
- 知识库已补盲区（独立 OAuth 域 lesson + 脚本指针），下次 search-first 即达。

---

# Wave 16m（2026-09-10：PR/issue 全景审核轮）

## 合并（6，全绿 + 内容审核通过）
| PR | 内容 | merge | issue 闭环 |
|---|---|---|---|
| #1594 zsxh | lessons.json schema CI 校验 + misakanet-index deprecation（#1374 防再犯）| 84339fb9 | #1374 ✅ |
| #1595 zsxh | Glama listing 校验脚本 | f357d83f | #1063 ✅ |
| #1596 zsxh | DSH listing 校验脚本 | ff6405c9 | #1065 ✅ |
| #1597 zsxh | memory dump watcher（misakanet/watcher.py，#1166）| 5403e28c | #1166 ✅ |
| #1599 zsxh | intake contributor 归属（#1342）| cd22710a | #1342 ✅ |
| #1586 zsxh | gap→lesson cleanup（去重完成，但**冲突未清**）| ⏳ 等再 rebase | #1567 待关 |

zsxh 本批把 4 个长尾 backlog issue（#1374/#1063/#1065/#1166/#1342）一次清掉——
registry 验证类（#1063/#1065）与 watcher（#1166）、contributor（#1342）全部交付。

## 剩余 open PR（4）
- #1513（kveita badge）：dco/audit 红，等作者补签（已催 2 次）
- #1538（s6pa1rta3n-lab error-signature）：4 项红 + draft + conflicting，作者今天活跃
  却未响应催——#1527 bounty 悬置；再等一轮后按 stale 处置
- #1544（intake receipt）：暂缓（用户拍板，保持 draft）
- #1586（zsxh）：再 rebase 后可合

## 备注
- #1594 与本仓"防再犯"主题一致（误用生成器 → CI schema 校验 + deprecation）

---

# Wave 16n（2026-09-10：stale 处置 + bounty 重开）

- **#1538 closed as stale**：error-signature 索引 PR 自 09-07 无更新（draft、audit-shape/
  CodeQL/DCO 四项红、base 落后已 conflicting），09-07 shape-guard 提示 + 09-09 维护者
  跟进两次均无响应（作者当日合了 5 个 lesson PR 却未触碰本 PR）→ 按 stale 关闭，留言
  含重新提交指引（从最新 main 起分支、先本地 gate 再推、避免测试粘贴 markdown）。
- **#1527 bounty 重新开放认领**：留言说明原 PR stale 关闭 + 任务范围（Phase A 索引 /
  Phase B 写入时提取）+ /claim 流程 + 与 #1562（词面鸿沟，已独立定位）互补关系。
- **#1586**：仍 dirty（zsxh rebase 未完全解决）；已给具体冲突指引，等其再推。

## 当前 open PR 仅 3
#1513（kveita，等补签）｜ #1544（暂缓）｜ #1586（等 zsxh rebase）

---

# Wave 16o（2026-09-10：MCP structuredContent 修复 + PR/issue 新一轮审核）

## 合并（4）
| PR | 内容 | merge | 备注 |
|---|---|---|---|
| #1586 zsxh | gap→lesson lifecycle（fin. 去重+rebase）| 2d243729 | #1567 ✅closed |
| #1513 kveita | Smithery badge 自动更新（作者补签后转绿）| 2e8070ca | 首 PR 作者终于补签 |
| #1603 zsxh | intake 统计评估（100 分层样本 + 测试）| b8a9b0e2 | — |
| **#1606（维护者）** | **MCP tools/call 补 structuredContent（修 #1605）** | f8504728 | 见下 |

## #1605 关键 bug 修复（本会话自产 + 自修）
DSH/cordis harness 对 MCP 工具输出做结构化校验，`misakanet_search` 报
`missing required property "value.structuredContent"` → 该类客户端搜索工具完全不可用。
修：`tools/call` 结果按 MCP 2025-06-18 同时返回 `structuredContent`（非对象结果包
`{value}`）；测试 workers/mcp-structured-output.test.mjs（3 例）+ MCP 套件 34 pass。
合并后自动部署（deploy workflow）。

## issue 处置
- **#1602 closed（duplicate of #1523）**：同用户同标题重复。
- **#1523 已答复**：MisakaNet **无货币支付**（荣誉激励；`docs/prd/02-wallet-bounty.md`
  为未实现 PRD；bounty 标签仅可见性，zero-bounty 任务明确 0 金额）——请提问者提供
  相反承诺来源以便澄清。
- 新 intake：#1600（OpenClaw 8.2 sqlite）、#1601（[Lesson] Windows Node sandbox）、
  #1605（已修）进入正常 triage 流。

## 待办
- #1604（zsxh shape-guard 测试文件豁免）：已留言去 data churn + rebase，等作者
- #1544 暂缓；bounty 池 #1527（已重开认领）

---

# Wave 16p（2026-09-10：致谢漏做 → 回执 SOP 机制化）

- **用户指出**：#1605 修复后由 `Fixes` 自动关闭，**没给报料者任何致谢/回执**（报料者
  是匿名 MCP 的 dsh agent，事后无从知晓贡献被采纳）。
- **补救**：
  1. #1605 补发致谢评论：致谢来源 + 根因 + 修复 sha + **报料者可自查的验证方式** +
     回执渠道说明（匿名收不到通知 → 公开留档；注册节点可获回执通道）。
  2. #1528（回执 bounty）补真实案例评论：回执缺失的代价 + 对实现的新增要求
     （**必须覆盖匿名 MCP intake**；可用"同 source 重提交返回 resolved"机制，与 issue
     正文已有的 re-submit 返回 answer 同源）。
- **机制化**：新建 `docs/maintainer/intake-triage.md`——intake 处置 SOP，含
  **§3 铁律：修复/答复后必须回执致谢**（模板 5 要素）+ 反例记录（#1605）+ 分类矩阵
  （含真盲区 vs 伪盲区判定，防写重复课）。
- **教训**：自动关闭 ≠ 闭环；飞轮断在"最后一环"（回执）。此条已写入 SOP 防再犯。

---

# Wave 16q（2026-09-10：release-please 诊断修复 + 反哺盘点 + issue 分类）

## 1. release-please：**不正常**（已定位 + 修复）
- 症状：workflow 每次 success 但**不发 tag、不开 release PR**；releases 停在 v2.27.1，
  tags 到 v2.28.0，manifest 却 2.28.1（状态三头错乱）。
- 根因（run 日志）：`Found pull request #1480: 'chore(main): release 2.28.0'` +
  **`⚠ There are untagged, merged release PRs outstanding - aborting`**——
  release PR #1480（09-05 合并）的 **GitHub Release 从未创建**（workflow 里
  `skip-github-release: true` + 后续 `gh release create` 步骤依赖 tag_name 输出，
  实际 skipped）→ release-please 判定"有已合并未打 release 的 release PR"，此后**每次
  run 直接 abort**。
- 修复：**补建 v2.28.0 的 GitHub Release**（tag 已存在，指向 e5bdb578）。
- 遗留缺陷（可后续修）：workflow 里 `steps.release.outputs.release_created` 在
  skip-github-release 下恒为 false → "Sync version to docs"/"Commit version sync"
  两步是**死代码**（docs 版本号不会自动同步）。
- 待验证：下次 push 触发 run 后应恢复（生成 release PR / tag）。

## 2. #1550 误关 → 自动守护（第三次不会再发生）
- 时间线：#1554（09-08）→ 手工重开 → **#1560 合并（09-09）再次误关** → 用户手工重开。
- 修复：新增 `.github/workflows/protect-pinned-issues.yml`（PR #1607 merged d22c3f4e）：
  `issues: closed` 时若编号在 `PROTECTED="1550,1258"` → **自动 reopen + 说明评论**；
  真要关闭时先从列表移除（注释写明）。

## 3. 被关闭 PR 的内容/知识反哺盘点
`is:pr is:closed is:unmerged` 共 **449** 个历史 PR。逐项核验近期有实质内容的 16 项：

| 组 | 结论 |
|---|---|
| #1158 hybrid weights / #1160 PR Genius 配置 / #1161 changelog / #1276 progressive disclosure / #1278 KV 诊断日志 / #1279 gap analysis / #1281+#1399 provenance / #1320 fallback 测试 / #1367 FTS5 / #1372 D1 usage / #1434+#1444+#1468+#1497+#1514 dsh 测试套件 / #1469 intake-kind 单源 / #1470 sync 测试 / #1443 evidence_refs / #1451+#1452 Vitest+WebCrypto lessons / #1535 canonical dedupe | **全部 ✅ 已在 main**（后续 PR 或等价实现）|
| #1538（error-signature）| 内容未落地，但**已转 #1527 bounty 重开认领**（含任务范围上下文），无知识丢失 |
| #1544（intake receipt）| 暂缓（用户拍板）；路线与实现要求已记录在 #1528 |
| #1541/42/51/1575-79/1589-93/1559（模板 bot/文档冒充）| **无实质内容**，按 triage 关闭 |

→ 结论：**知识反哺无遗漏**；"关闭而不丢失"实践有效。

## 4. issue 分类与放出（33 open → 已处置）
- **关闭噪音**：#1552（`test error` 无语义）→ closed。
- **放出给 agent/爬虫（16 个）**：加 `agent-friendly` + `good first issue`，去掉
  `needs-human-review`（判定：这些 intake 是**可写 lesson 的素材**，真盲区判定可机器完成，
  无需人工确认）——#1472/#1473/#1477/#1499/#1501/#1504/#1548/#1600/#1460/#1427
  （priority:high intake）+ #1553/#1555/#1574（salvage 池）+ #1130/#1145/#1146（本地环境类）。
- **保留人工复核（真需人）**：#1459（生产 MCP auth 架构决策）、#1396/#1397（葡语 question
  需人工答）、#1376（需真人 Cloudflare 凭据的一次性运维）、#1170（M-900iB 特定环境）。
- **待审 lesson 提交**：#1500/#1547/#1601（pending-review）。
- bounty 池：#1550（试点 2/20 进行中）、#1527（重开认领）、#869（Glama 路由）。

## 5. 待办清单（本轮结束快照）
**即时**：release-please 验证（本 push 后看 run）；#1604 等 zsxh rebase；#1544 暂缓。
**issue**：16 个已放出等认领；真需人工 5 个（见上）；#1523 已答待关（提问者确认后关）。
**飞轮**：#1550 试点推进（2/20）、#1527 认领；failure_harvest 接 ci-lesson-search 真实源。
**人环**：#1550 社媒/社群转发；MCPVault claim（Henrik 邀请）；roof4u adoption case 文档。
**技术债**：release-please workflow 的 skip-github-release 组合（docs 同步死代码）。

---

# Wave 16r（2026-09-10：release-please 修复确认）

- **根因精确化**（Wave 16q 的初判不完整）：release-please 判定 "untagged merged release
  PR" 用的是**它自己的标签状态机**，不是 GitHub Release 对象——release PR #1480
  （`chore(main): release 2.28.0`）合并后标签**一直停在 `autorelease: pending`**
  （release 完成步骤从未成功），因此 release-please 每次 run 都在
  `Found pull request #1480 … aborting` 处中止。
- **修复两步**：
  1. 补建 v2.28.0 的 GitHub Release（tag 已存在）——补账
  2. **把 #1480 标签由 `autorelease: pending` 改为 `autorelease: tagged`**——解除状态机卡死
- **验证**：重跑 release-please（run #415 rerun）→ **生成 release PR #1608
  `chore(main): release 2.29.0`** —— 链路恢复 ✓
- **待决定**：#1608 是否合并（合并 = 打 tag v2.29.0 + GitHub Release；PyPI/registry 仍走
  各自的 dispatch 流程，不受影响）。
- **技术债**（未修）：workflow 中 `skip-github-release: true` 与后续
  `steps.release.outputs.release_created` 组合导致 "Sync version to docs"/"Commit version
  sync" 恒被跳过（死代码）——`autorelease: pending` 卡死的根因也与该组合有关（release 完成
  路径未被真正执行）。建议后续改为：去掉 skip、用有 releases 权限的 PAT（或保留 skip 但
  改为依赖 `tag_name` 而非 `release_created`）。

---

# Wave 16s（2026-09-10：README badge 修复 + HOL 报告要点）

## 1. Smithery badge "resource not found"（真 bug，PR #1609 merged 3d001953）
- 根因：README 用 shields.io endpoint badge 读 `data/badges/smithery.json`，而该文件
  **从未生成**——`update-smithery-badge.yml`（#1513）**一次都没跑过**；手动 dispatch 后
  发现 `git checkout data` **静默失败**（只 fetch main、fetch-depth: 1 → 无本地 data 分支）
  → commit 落在 main、rebase 重写分支、push 被拒 → 文件永远缺失。
- 修复：① API 补上 `data/badges/smithery.json`（Kin score **82/100**，脚本本地实测抓到）；
  ② workflow 改为 `git fetch origin data --depth=1` + `checkout -B data origin/data` +
  `git push origin HEAD:data` + 末尾断言文件已在 data 分支。

## 2. HOL badge 风格不一致（同 PR）
- 根因：URL 里 `style=for-the-badge` 是**传给 hol.org API** 的（非 shields 参数），返回
  for-the-badge 风格 JSON（渲染 `MISAKANET: 62`）；且 hol.org 实时 API 经 shields 读取
  不稳定（实测 `custom badge: inaccessible`）。
- 修复：改**静态 flat shield**，链接仍指向 hol.org（其列表检测器在 README 找该 badge，
  报告明确 "+2% trust"）→ 风格统一且不丢分。

## 3. HOL 报告其他要点（待办，未处理）
- **CI 无安全 scanner** → 列表 trust **-10%**（报告建议在源仓 CI 维护 scanner）。需调研
  HOL 支持的 scanner 形态后落地。
- 缺 `websiteURL` / `privacyPolicyURL` / `termsOfServiceURL`（publishability medium×3）
  → 可在 registry 元数据（server.json / 列表信息）补齐。
- `skill-scan.unavailable`：`scripts/` 100+ 脚本缺"权限声明"（Cisco skill scanner 要求）
  且 scanner 超时（60s）——工作量大、优先级低。
- 报告亦确认：**HOL verified badge 已在 README（+2% trust）**、owner verified（@Ikalus1988）。

---

# Wave 16t（2026-09-10：Dependabot 修复 + HOL scanner 接入（第一性原理结论修正）+ 扫描首轮分析）

## 1. Dependabot #21（high）→ fixed（PR #1610 merged d65936e4）
- 警报：`sharp: libheif` GHSA-rgj7-g3m4-5g8c（影响 `sharp < 0.35.4`）
- 本仓：`sharp` 是 **dev-only 传递依赖**（wrangler → miniflare），被 `overrides` 钉在 0.35.3
- 修：override → **0.35.4** + lock（sharp 0.35.4 / @img/sharp-libvips 1.3.3）
- 验证：**Dependabot 自动转为 `fixed`** ✅（本地 npm audit 不可用：镜像未实现 audit 端点）

## 2. HOL "continuous scanner CI" 调研 → 接入（PR #1611 merged 443ec2b4）
**第一性原理结论修正**：上轮判断"不值得为仪表盘分数加 scanner"；调研后发现 HOL 认可的
机制（Guarded Repository）**附带我们确实没有的能力**，故接入：
- 机制：调用 HOL 官方 reusable workflow；verifier 用 GitHub OIDC
  `job_workflow_ref`/`job_workflow_sha` 校验；产出 **GitHub 签名 attestation**
- 证据只上报粗粒度元数据（commit/run id/scanner 版本/score/grade/最大严重度/finding 数/
  SARIF 摘要）——**不含源码/prompt/凭据**
- 我们的接入：profile=default、fail_on_severity=critical、upload_sarif=true、
  visibility=public、触发=main push+每周+手动（**不卡 PR**）、pin tag v3.0.148

## 3. 首轮扫描结果（run #1 success，attestation 已签名 + 注册）
`SCORE 50 / GRADE F / MAX_SEVERITY high / FINDINGS_TOTAL 144` —— 构成分析（144 条 open alerts）：

| 规则 | 数量 | 判定 |
|---|---|---|
| `GITHUB_ACTION_UNPINNED` (warning) | **120** | 供应链卫生建议（action 未 pin SHA）——低危，全面 pin 成本高、收益仅分数 |
| **`HARDCODED_SECRET` (error)** | **20** | **全部误报**（逐条核查：release notes 的展示文案、lesson 讲 `--api-key` 泄露的正文、测试占位/脱敏 fixture）——本地对真实密钥模式全仓扫描**零匹配，无泄露** ✅ |
| `PLUGIN_JSON_*` (3) | `.codex-plugin/plugin.json` 缺失/无效/字段未校验 | **可低成本改善**（codex registry 清单） |
| `CODEXIGNORE_MISSING` (note) | 1 | 缺 `.codexignore` |

**关键结论**：scanner **没有报出任何 prompt-injection findings** → **路径 2（prompt-injection
防护）仍需自研**（该 scanner 不覆盖 lessons 内容注入面）；grade F 的主体是噪音
（unpinned action + 密钥误报），**不值得为分数做大规模改造**。

## 4. 待办
- 低成本高分项：补 `.codex-plugin/plugin.json` + `.codexignore`（消除 4 条 findings）
- 可选：只 pin 第三方 action（非 actions/*）降低 unpinned 噪音；或加 scanner baseline
  （注意 reusable workflow 的 `trust_repository_policy` 默认 false，仓库侧 baseline 可能不被采信）
- **路径 2 自研**：intake/邮件/lesson → agent 工作流的 prompt-injection 防护（scanner 未覆盖）

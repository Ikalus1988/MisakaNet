# 评审评估与策略（2026-09-12）

> 触发问题：**现状是否需要评审？最好的评审策略是什么？是否需要增加赏金 issue？**（博客之外还欠什么）
>
> 结论先说：**需要，而且优先级高于新增功能。** 依据不是感觉，是当天三条可验证的证据（§1）。
> 评审的重心应从"更多检查"转向**补上无人覆盖的失效面**（§2 Tier 1）与**信任语义的独立复核**（§2 Tier 2）。

---

## 1. 现状：三件"没人看所以一直坏"的事

### 1.1 一个每天在跑的 workflow，连续 4 天失败，无人发现（今天修完）

`Intake Salvage Digest`（每日 08:00 UTC）自 **≤2026-09-09** 起每天失败。排查后发现有**三层 bug 叠在一起**，
每一层都只在上一层被修掉后才露出来：

| # | 缺陷 | 症状 | 根因 |
|---|---|---|---|
| 1 | `gh` 没有 token | `exit code 4` | 只声明了 `permissions: issues: write`（**不会**把 token 放进环境），仓库里其它用 `gh` 的 workflow 全都显式 `env: GH_TOKEN` |
| 2 | `$(date -u +%Y-%m-%d %H:%M UTC)` 未加引号 | `date: extra operand '%H:%M'`，`exit 1` | 空格让 `%H:%M` 成为第二个操作数；步骤在 `bash -e` 下直接中止 |
| 3 | 双引号里的反引号 | ``/salvage-review: No such file or directory``，`exit 127` | `` `/salvage-review` `` 被当作**命令替换**执行；同一坑还静默吃掉了 `` `## Error` ``/`` `## Verification` `` 两段文案 |

**代价是"载荷性"的**：这个 job 的存在意义就是把 auto-rejected 的 intake 送到人眼前。它 4 天没跑，
于是 salvage 队列（4 条）一直躺着没人处理——而 Actions 列表里只多了一条"每日计划任务失败"，
在几十条绿色里读起来像噪声。

**教训**：仓库里**没有任何测试**覆盖 workflow 的 shell 脚本。YAML lint、pin 检查、diff review 都抓不到这三条。
修完之后补了 `tests/test_salvage_digest_script.py`：把该 step 从 workflow 里抽出来、替换 `${{ … }}`、
用 stub `gh` 在 `bash -e` 下跑一遍（0.2 秒）。三处 bug 任意放回都会红（3/3/1 例失败）。
线上核验：手动 `workflow_dispatch` → 首个绿色 run（`828707962`），并生成摘要 **#1639**。

### 1.2 今天的改动全部是"自审自合"

今天合入：检索语料截断修复、rich 投影、intake 评分判据、release 版本不变量、15 个测试夹具的 token 迁移、
三条投稿课程。全部由同一个执行者写、测、合——**没有第二双眼睛**。两个直接后果：

- 我加的 R6/R1 不变量差点让**发布 PR 永久变红**（release-please 不会提升 `.well-known` 卡片与 `glama.json`），
  直到今天查 `audit` 为什么红才发现；
- 我给 workflow 插注释时把 `run: |` 块缩进弄坏，直接推到 main，`workflow_dispatch` 返回 422 才暴露。

测试是有效的（都能复现），但**测试与实现出自同一个理解** —— 共同盲区不会被测试抓到。

### 1.3 贡献者侧积压：8 个 PR / 20 条 intake（**§5 已处理，收盘剩 2 个 PR**）

- **先纠正我自己的一个错误判断**：我最初把 #1528 的情况说成"4 份实现、3 位作者在重复劳动"，**按内容核对后是错的**。
  真实构成：**1 份实质实现**（#1544，draft，17 文件 / +7468）+ **2 个占位 PR**（#1634/#1616：各新增
  `solutions/fix_issue_1528.py`，内容是**一个随机小数** `0.012724685482680798`）+ 1 个同类占位（#1617，对应 #1527）。
  即：不是"重复劳动"，是**赏金刷单**。只看 CI 状态（它们只是 `dco`/`audit` 红）永远看不出这一点——必须打开 diff 看内容。
- 其余：#1604（真 PR：6 个 commit 缺 DCO + 携带生成物）、#1615（README 1 行 badge 修正）、
  #1633（贡献者课程，25 项检查全绿，等内容复核）；
- 20 条 intake 里，4 条在 salvage 队列（现在有 #1639 摘要做入口），1 条缺 AC（#1635）。

---

## 2. 评审策略：按"能抓到什么"分层，而不是按"谁写的"

### Tier 0 — 已在跑，保持（机械门禁）

DCO、pytest、worker 套件、计数/页面/版本门禁、SARIF（现在 0 开放告警）、action pin 检查。
它们的边界是**"能写成断言的"**：能证明"代码符合某条规则"，不能证明"某个东西真的在运行"。

### Tier 1 — 补上"可执行表面"（今天新增的类别，性价比最高）

> 判据：**"绿"不等于"在跑"。**

1. **把 workflow 的 step 变成可在本地跑的测试**：凡是 workflow 里带 `run: |` 的关键脚本，
   抽出来 + stub 掉外部命令 + `bash -e` 跑一遍。今天已落地一例（salvage digest）；
   建议下一步做成通用检查（`scripts/check_workflow_scripts.py`），覆盖 `update-lessons`、`register`、
   `issue-quality-gate` 等每日/每 PR 跑的脚本。
2. **把外部平台状态变成"声明 + 定期核对"**：dsh.so / DSH STORE 的裁决、
   `registry/candidates.json` 的 `latestCommit`/`status`、registry 的 `isLatest`。
   今天就是因为"钉在修复前的 commit"而不自知。
3. **每周一次"看不见的表面"巡检**（产出 3 行结论即可）：
   ① 所有 `schedule` workflow 最近一次结论（今天证明它们会静默失败）；
   ② 开放告警数（code-scanning / Dependabot）；
   ③ 外部平台状态是否与仓库声明一致。

### Tier 2 — 信任语义的**独立**复核（最高价值，必须"不是作者"）

凡改动**"什么被自动接受 / 拒绝 / 展示"**的东西，都要一个没有作者包袱的人过一遍。今天属于这一类的三处：

| 改动 | 决定什么 | 为什么必须独立复核 |
|---|---|---|
| intake 评分（`intake_auto_review.py`） | 什么被 auto-reject（= 什么永远进不了人眼） | 阈值直接决定"真盲区是否被挡在门外"；今天的修复正是一次假阳性事故 |
| 检索相关性下限 + `no_match` | agent 看到什么、看不到什么 | 静默的假阴性不会有人报错，只会让人不再用 |
| release 版本不变量 + `release-please` 接线 | 能不能发版 | 今天的实例：一条不变量让发布 PR 变红了一整天 |

**评审方式建议：对抗式复核（adversarial review）**，要求复核对每个改动给出三样东西：
一个假阴性用例、一个假阳性用例、一条"修了但没生效"的路径（缓存/门条件/触发时机）。
具体做法：给复核者 **diff + 测试 + 线上可观测点**，但不给"我认为它怎么工作"的说明，避免复述我的理解。
（`subagent` 可实现：它没有本次会话的上下文，正好充当"只读代码"的复核者。）

### Tier 3 — 贡献者侧评审：按"卡住别人的程度"排队

1. **一句话解围**：#1634/#1616/#1617 的作者缺 `Signed-off-by` → 留评论给命令；
2. **止住重复劳动**：#1528 的四份实现选一（按质量/完整性），其余明确关闭并致谢；
3. **内容复核**：#1633 按 `intake-triage.md` 的课程清单过一遍（结构、证据分级、验证段可跑）；
4. **批量 triage**：以 #1639 摘要为入口处理 4 条 salvage；#1635 补 AC。

### Tier 4 — 我自己的动作纪律（成本最低，今天已踩两次）

- 改 workflow / 门禁类文件后，**先跑本地对应测试再 push**（今天我 push 了坏 YAML，靠 422 才发现）；
- 新增一条不变量时，**先回答"谁在写这个事实"**（§12 已写进残余风险）——
  发布了不变量却没接上"写入者"，第一个撞上的就是发布 PR。

---

## 3. 赏金 issue：要发，但只发"可判定"的，并先做防重复

### 3.1 先看现有赏金教了我们什么

bounty #1528 引来了 **4 份实现（3 位作者）**。这不是作者的问题，是**赏金设计**的问题：

- 规格不够"可判定"（"给贡献者回执"有太多合理解释）→ 各人按自己的理解实现，无法比较；
- 没有认领机制 → 三个人并行开工，谁也不知道别人在做。

### 3.2 建议的赏金规则（下次发之前先定）

1. **可判定验收**：issue 里给出"跑什么命令、看什么输出、什么算通过"，像今天的新测试那样可复现；
2. **一人一实现**：用 Opire 的 `/try` 认领，并在 issue 顶部维护"当前认领人"；
   认领后 7 天无 push 视为释放；
3. **先 spec 后实现**（针对中等复杂度）：先用一条"方案评论"征集（不付赏金或小额），
   选定方案后再发实现赏金——避免四份并存；
4. **不发给需要领域判断或涉及信任语义的**：salvage 课程化、检索下限标定、页面 URL 策略、E0→E2 证据升级。

### 3.3 具体候选（按"值不值"排序）

| 候选 | 为什么适合赏金 | 验收方式 |
|---|---|---|
| **`scripts/check_workflow_scripts.py`**：把"workflow 里的关键脚本能在本地跑"变成通用检查 | 规格明确、纯机械、正是今天暴露的缺口 | 新测试对每个含 `run:` 的 workflow 做静态+可执行检查；把三处 bug 放回会红 |
| **`docs/data/lessons.json` 镜像 nightly 检查** | 小、边界清楚（§12.14） | 一个 nightly workflow + 失败即开 issue |
| **把 #1617（error-signature index）收口** | 已有实现，只差 DCO/复核 —— **不宜另开赏金**，应把它推完 | 通过语料质量检查 + 基准指标对比 |
| ~~salvage 4 条课程化~~ | 需要领域判断 | 不发赏金，维护者做 |
| ~~IDF 加权覆盖率下限~~ | 需要真实查询集标定（§11.8 有净负收益前例） | 不发赏金，维护者做 |

---

## 4. 仓库待办（不含写博客），按"是否阻塞别人 / 是否需要人决定"重排

### A. 阻塞别人，且已经可动（今天多数已修，剩 3 条）

| 项 | 状态 |
|---|---|
| 发布 PR #1631（2.30.0） | ✅ 已解阻（`334f27479`，21/21 绿）→ **等你决定合并** |
| salvage digest（#1639） | ✅ 已修并线上跑通（今天首个绿色 run） |
| 4 份重复实现的 bounty #1528 | ⏳ 选一、关其余、逐一回执 |
| 3 个 PR 缺 DCO（#1634/#1616/#1617） | ⏳ 留评论给命令即可解围 |
| #1615（badge 修正，`auto-merge` 红）、#1604（只有 2 条 check） | ⏳ 需人看一下为什么 CI 没跑全 |

### B. 语料与队列

- 4 条 salvage（#1630/#1574/#1555/#1553）→ 人工课程化（修好的评分器复评仍是 21–36 分，说明**正文确实薄**，不该靠调阈值）；
- #1635 补 AC；其余 16 条 high intake triage（以 #1639 为入口）；
- #1500 的 Go 课程升 E2（需要有人跑 `golangci-lint` 并把输出贴回）。

### C. 检索质量

- **IDF 加权覆盖率下限**：让"语料里确实没有"的查询归零 + 走 `no_match`，而不是返回邻居课（必须先用真实查询集标定）；
- `#1562` error-signature 索引 = PR #1617（先解 DCO）。

### D. 外部平台

- dsh.so L5 重扫 + DSH STORE 复检（已在其 #747 留言请求按新 commit 复检；双方仍钉在修复前的 `6d63642e4`）；
- AAIF 第三篇。

### E. 维护性 / 需人决定

1. **审计报告里显示的分数会误导**：判定用 `weighted_score`，显示的是 `weighted × confidence`（confidence 在不等式两侧约掉，只影响显示）→ #1635 显示 38.5/100 却判 review（阈值 40）；
2. `release-please.yml` 的 `Sync version to docs` / `Commit version sync` 挂在 `release_created` 上，而该 workflow 设了 `skip-github-release: true` → 抽查的 run 里都是 skipped（**需人决定**用哪个条件）；
3. **页面 URL 策略**：slug 由 title 生成，标题一改旧 URL 就孤儿化（现由 manifest 自动清理）→ 是否改用稳定 `id` + 跳转页（编辑决策）；
4. `docs/json-ld-schema.md` 示例里的计数属示范文本，故意未纳入 SSOT；
5. `failure_harvest` 接真实事件源。

### F. 建议新增（来自本次评审）

- **Tier 1 通用化**：`scripts/check_workflow_scripts.py`（见 §3.3），把今天这一课变成仓库的常设检查；
- **每周巡检**：schedule workflow 结论 + 告警计数 + 外部平台状态，3 行结论贴进最近一期 handoff。

---

## 5. 执行结果（2026-09-12，按 §3 的顺序做完）

### ① 发布：2.30.0 已合并并**真的发布出去**（不只是 PR 变绿）

- 合并 #1631（merge commit `415a1c950`，23/23 检查绿）→ tag **v2.30.0** 已建；
- 但紧随其后的 run **失败**：`Dispatch the PyPI publish` 报
  `HTTP 403: Resource not accessible by personal access token`。
  根因：`secrets.SHELDON_PAT || secrets.GITHUB_TOKEN` **只在 secret 为空时**回退；PAT 有值但缺 `actions: write`，
  于是有 `actions: write` 的内置 token 从未被尝试，**PyPI 没收到 2.30.0**，后面两步（docs 徽章同步 + 提交）也被跳过。
  已修（`8f9327fdd`：显式重试内置 token）+ 手动补发（PyPI 现为 **2.30.0**）+ 手动跑掉被跳过的 docs 同步；
- 顺手发现**站点徽章在我手动同步时被我弄坏**（`V=` 写成了别的变量名 → `$VERSION` 为空 → 徽章变成裸 `v`）并推上了 main。
  已修（`837f14cb3`）：修回 `v2.30.0`，并给 `align_versions.py` 加 **R8**（站点徽章必须唯一且等于 manifest 版本）
  + 测试。**说明**：仓库里其实**已有**一个"徽章必须存在"的断言，我之所以没被拦住，是因为**没跑测试就 push**——
  缺的是纪律，不是门禁；R8 补的是"等于 manifest"这条真正没被覆盖的规则。
- 线上核验：`misakanet.org` 徽章 = **v2.30.0** ✓，PyPI `latest` = **2.30.0** ✓。

### ② 独立对抗式复核：进行中

已派出一个**没有本次会话上下文**的复核者（避免继承我的假设），要求对四块（检索 / intake 评分 / 版本不变量 /
CI 脚本）各给出：**一个假阴性、一个假阳性、一条"修了但没生效"的路径**，并附实际命令与输出。结论回来后并入本文档。

### ③ 贡献者侧：8 个 PR → **2 个**，salvage 4 → 3

| 动作 | 对象 | 结果 |
|---|---|---|
| 合并 | #1615（README badge URL，`dco`/`audit` 绿，两条 URL 实测 200） | ✓ `32cbd340a` |
| 合并 | #1633（葡萄牙语证呼/提及消歧课程，实质课程） | ✓ `3a7499b90`，语料 388 → **389**（索引/页面/计数已一并再生 `e8832a5fc`） |
| 关闭（占位） | #1634 / #1617 / #1616 | 各留回执说明"内容是随机数、无实现"，并写清重新提交的正确姿势 |
| 回执关闭 | #1630（salvage 队列项，已被 #1633 课程覆盖） | ✓ 队列 4 → **3** |
| 评审意见 | #1604 | 要求补 DCO（6 个 commit）+ 去掉手工携带的生成物（否则回退已合并课程，#1374 同类事故）；实质改动仅 `pr-shape-guard.yml` |
| 评审意见 | #1544 | 按 bounty 验收标准提三点：可判定端到端演示、幂等（回执重试不得重复发帖）、不碰生成物；合入前不要转正 |

### ④ 赏金：新建 1 个、收口 1 个

- **新建 #1640**：`scripts/check_workflow_scripts.py` —— 把"workflow 的 shell 步骤能在本地跑"变成常设检查。
  规格按"可判定"写：5 条静态规则（各带 rule id）、可执行冒烟白名单、**必须双向证明**
  （每个规则各一个反例 fixture 必须报出、真实仓库必须 0 findings、三层历史 bug 各还原一次要分别命中
  `W2-gh-token`/`W5-date-quote`/`W4-backtick`），并明确列出"直接关闭、不进入评审"的形态（占位实现、手改生成物、缺 DCO、只贴 CI 绿灯）。
- **收口 #1528**：在 issue 里公开盘点（1 份实质 + 2 份占位）、写明认领规则（`/try` 认领、7 天无 push 释放、一个 bounty 只留一个 PR），
  并把"赏金刷单"的具体证据（随机数文件）贴出来。

### ⑤ 这一步学到的两条（已写进 §1 与本节）

1. **只看 CI 状态无法识别刷单**：占位 PR 的 CI 只是 `dco` 红，看起来和"忘了签核的正常 PR"一样；**必须打开 diff 看内容**。
2. **"有门禁"与"跑了门禁"是两件事**：徽章那条我 push 前没跑测试，而门禁本来会拦住它。

---

## 6. 独立对抗式复核的结果（2026-09-12，复核者无本会话上下文）

复核者被要求对四块各交"一个假阴性 / 一个假阳性 / 一条修了但没生效的路径"，结论很硬，**九条 finding 里我逐条复核后确认全部成立**。
下面按处置状态分类，而不是按它给的编号。

### 6.1 已修（并证明）

| # | 结论 | 处置 |
|---|---|---|
| **F1/F2** | **生产 KV 写入全线失败**：`misakanet_register` / `/mcp/connect` / `/api/search-signal` / `/api/helpful` 均返回 CF `error code: 1101`；索引无法续期；匿名配额耗尽后既搜不了也注册不了 | 根因在**环境侧**（下面 6.2），但**"一次写失败就打掉整个端点"是我们自己的缺陷**：33 处 KV 写里 28 处是裸 `await ...put()`。已引入唯一写入器 `kvPut()`（不抛、记录、返回是否写入成功），限流计数器改为**失败放行**，register 不再发出"存不进去的 token"而是给出明确的临时失败 + 还能用什么，`refreshSearchIndex` 在写失败时返回 `{refreshed:false, reason:"kv write failed"}` 而不是谎报成功。新增 `workers/kv-write-failure.test.mjs`（6 例）钉住这个契约 |
| **F4** | 我在同一天合入的课程**自证失效**：它把 `npm ERESOLVE peer dependency conflict → no_match` 写进 Verification，而这段文字本身进了索引 → 该查询现在返回**这篇课程**（线上第 1 条，我已复现） | 已更正课程：保留"当时确实 no_match"，并把这个**自证失效机制**写成课程里最有价值的一节（写进索引的验证样例就不再是"语料外的对照"；合入前的验证描述的是合入前的语料）。另加"楼层仍然漏水"的诚实声明（见 F3） |
| **F5** | R8 的徽章**没有写入者**：`docs/index.html` 是纯字符串 extra-file，而 release-please 的 Generic updater 只改带 `x-release-please-version` 注解的行——本仓库一个注解都没有。模拟提示：**1 failed / 17 passed**，下一个发布 PR 会因 R8 变红（正是 R8 想防的症状） | 徽章行加注解 + 新增测试断言"恰好一行带注解且该行含版本"。**模拟 release 后 10 passed、`align_versions --check` 干净**（此前 R8 报错） |
| **F6** | `docs/.well-known/glama.json` **线上公开且陈旧**：版本 2.27.1（落后三个版本）、"320+ lessons"（实际 389），未纳入任何清单 | 修正版本与计数，纳入 release-please extra-files、`align_versions` 卡片清单（R6）、接线测试，并把该句式注册进**计数 SSOT**（写它的人） |
| **F9** | `/api/lessons` 泄露内部字段 `textMode`；fire-and-forget 的 `KV.put` 无 catch（未处理的 rejection） | `publicLessonRow()` 现在同时剔除 `indexText` 与 `textMode`；`kvPut()` 覆盖了那处未处理的调用。（"文档写 384 而语料 389"属历史叙述与提交信息，不追改） |

### 6.2 需要你（账号侧）决定的一件事

**KV 写入目前仍在失败**：修好我们的降级之后，`misakanet_register` 会诚实地报告 `storage: {node:false, token:false}` ——
也就是说**任何人现在都无法注册**（匿名 5/天仍可用）。我无法从本机查账号用量：`wrangler` 的登录态已过期
（`Not logged in. Your auth token has expired`），环境里也没有 `CLOUDFLARE_API_TOKEN`。

两个候选原因，需要你在 Cloudflare 控制台确认：
1. **KV 写入配额**（免费计划 1,000 写/天）：今天的流量（每日 cron + 每请求限流计数 + 我的验证 + 复核者的探测）足以打满；
2. KV 服务侧异常。

无论哪种，**"绿"的教训再次成立**：`/api/health` 之前一直回 `ok`，而每个需要存储的调用都在 500。现在它会报告
`kv_writes`（内存统计，零额外写入——用写来做健康检查本身就可能打满配额），`/api/search-index` 也会在 20 小时窗口外报 `stale`。

若确认是配额：可选的减写入手段是把"每请求一次限流计数"改成批量/内存聚合（限流本就是尽力而为），或升计划。这属产品决策，我没有擅自改。

### 6.3 仍然开着（下一轮按此顺序）

| # | 严重度 | 结论（我已线上复现） | 计划 |
|---|---|---|---|
| **F3** | 高 | **相关性下限对自然语言基本无效**：`how do I bake sourdough bread` 返回 5 条无关课程（只因 `how` 在语料里 df=5），`VISION_API_KEY env var not set` 的前 5 条全不相关、真正提到它的课程排第 9。机制：`required = max(1, min(2, pairable))` 在无法配对时退化为 1，且"有信息量"的门槛（df ≤ 30%）对常见英文词过于宽松 | 实现 **IDF 加权覆盖率下限**（匹配词要覆盖查询信息量的一部分，而不是"命中一个词就算命中"）。**必须用真实查询集标定**：正例（`docker exit code 137`、`kubectl crashloopbackoff`、`git push rejected non-fast-forward`）与负例（sourdough、VISION_API_KEY、随机串）各跑一遍，并写进测试 |
| **F7** | 中 | 新的内联证据规则**可刷分**：空话 + 装饰性证据（代码块、版本号、`fix:`、"verified"）得 51.0–69.5，**高于**它本来要救的那份真实报料（51.7）；同样文本**加上** `## Verification` 标题反而更低（64.5 vs 69.5）——结构更好的报料被惩罚 | 让分数取决于**可核验的内容**而不是形态：例如要求证据里出现具体标识（命令 + 输出/错误码）、对"关键词堆砌"降权、修掉"有标题反而更低"的倒挂 |
| **F8** | 中 | `gh` 鉴权 guard **既不灵又过敏**：漏掉 `if ! gh workflow run`、缩进的 `gh pr comment`、`env X=1 gh …`（本仓库 3 处真实漏检），在另一个仓库里**放行**了 3 处未鉴权的 gh 调用，却**误报**一个从不调用 gh、只是在 heredoc 里写了 gh 命令行的 step | 复刻复核者的两组 fixture 作为回归测试；把"提取 gh 调用"从正则升级为更结构化的判定（至少避开 heredoc/注释，并覆盖 `if`/`env`/间接调用），同时**明确其边界**：guard 是启发式，真正的保证来自 `tests/test_salvage_digest_script.py` 那种真跑 step 的测试 |

### 6.4 复核者明确**没能**推翻的部分（值得记下）

- 语料截断修复、rich `indexText` 投影、`docCount`/`textMode` 重建门条件、isolate memo 失效——**机制正确、测试充分**（15 例 + 它自己的独立探针）；
- `$..version` 在 `server.json` 上恰好解析到两个版本字段；2.30.0 的 bot 提交**真的**提升了 registry/卡片那一组；
- salvage digest 的三层 bug 诊断准确，且现在由**真跑 step** 的测试覆盖；运行历史与"每天失败"一致（实际自 ≥09-03）；
- PyPI dispatch 的重试配置正确（`actions: write` 在，重试没有被 `set -e` 吃掉）——但它指出**该路径尚未在真实发布中跑过**（14:17Z 那次因 tag 已存在而跳过）。

# MisakaNet handoff — 2026-09-05

Repo: `C:\Users\hp\MisakaNet`（本地工作树 /home/eric_jia/MisakaNet）
Maintainer: `Ikalus1988`
Session: 续接 `handoff-2026-09-03.md` §14（目标轮 2 结束后：CodeQL 清零 + release 2.27.x + bounty 审核 + PR 合并）

## 当前状态

- 远程 main 持续被 bot 提交推进（leaderboard/feed），HEAD 见下节；**本地落后远程**（缺 #1471 等外部会话提交），网络恢复后 `git fetch origin main && git reset --mixed origin/main`，勿 force-push。
- **CodeQL open alerts = 0**（advanced setup 接管）。
- Releases 已到 **v2.27.1**（24h 内 v2.24.0→2.27.1 五连发，流程全部走通）。
- 轮作 #1462 已被 agent 完成（#1471 merged）——事件驱动 answered 同步上线。

## 本会话完成（2026-09-04 ~ 09-05）

### CodeQL 告警全清零（含 UI 路线判断）
- 用户给的是 default setup 页面；**Custom configuration 是 UI-only 且无 REST** → 走 **advanced setup**（`.github/workflows/codeql.yml`，commit `5cd84259`）：config-file 引用 `.github/codeql/codeql-config.yml`、security-extended、无 autobuild（python 包 pip 会拉 torch）。
- **#68**（check_worker_secrets FP）：query-filters exclude **实测不生效**（config 加载成功但仍报）→ 改**顶层 `paths-ignore`**（分析级排除）→ fixed。config 注释已刷新（`02dfdba0`）。
- **#69**（dsh 测试 shell 注入）：`fs.cpSync` 替代 `cp -r`（`2064e3ae`）→ fixed。
- security-extended 新扫出 #70-79：**#70** 删多余 `os.chmod`（test_ci_self_heal）；**#77-79** fatal-guard ×2 + docs 示例改 `mkdtempSync`；**#71-76** fatal-guard 注入 = 设计契约（sanitizeCommand+shell:false，#67 历史）→ dismiss（reason 用 `false positive`，**注释 ≤280 字符**；`won't fix` 写法的转义曾致 422）。全部 fixed/dismissed（`412eccbb`）。
- 教训：GitHub alerts dismiss PATCH `/code-scanning/alerts/{n}`；同一 finding 若再出现会新建 alert；dismiss 注释限 280 字符。

### Release v2.27.0 / v2.27.1
- **v2.27.0**（#1467）：amend-sign-off → merge（`eecf939c`）→ tag+Release → 对齐（server.json/glama×2/worker default 2.27.0）。
- **v2.27.1**（#1479，由 #1478+#1293 的 fix 触发）：同流程 → merge（`22dd84df`）→ tag+Release → 对齐 2.27.1。
- release-please 每 merge 一个 fix/feat 就开新 release PR——**常态是逐个立即消化**（amend 补签 → merge → tag/Release → 版本对齐），不要积压。

### bounty 竞争 PR 审核（关 5 + 关 issue #1401）
| PR | 判定 | 处置 |
|---|---|---|
| #1468（JAGAT Auto-Patch 修 #1403） | MockMCPClient 假集成 + ~700 行无关数据噪音（lessons/leaderboard/feed） | 关（无奖励转出） |
| #1474（dsh install guide） | #1402 已由 #1422 交付，docs/integrations/dsh.md 已在 main | 关（重复） |
| #1475（integration suite，#1403 晚到） | #1414 领先（真集成）单赢家政策 | 关 |
| #1476（install-method tests，#1401） | 复制 main 已合并测试（#1424） | 关 |
| #1444（#1401 旧 dup） | 同上 | 关 |
| **#1401 issue** | #1424 已合（三安装方法测试在 main） | **closed（delivered）** |

### PR 合并
- **#1478**（hjqcan，mcp_http_server 迁移 MCP SDK v2）：patch +4/-6 极干净、未动 question/kind 逻辑、全绿 → merged。
- **#1293**（hjqcan 改名 test(mcp)）：缩为 Bearer-token 提交回归测试 + 文档，仅 audit 噪音 → merged。
- **#1414**（#1403 领先）：作者修一轮后 **Node 矩阵（18/20/22×多 OS）仍全挂** + 末提交未签 → 已 nudge，继续等作者。
- **#1469/#1470**（ghzhost，轮作 #1463/#1464）：内容已批准、CI 绿（仅 audit），**等作者补 DCO**（fork 需作者 amend --signoff）。

### 「其他内容」巡检发现
- **轮作闭环实证**：#1462 事件驱动 answered 同步已由 agent 完成——**#1471 merged（ecc68ffadd）**：`sync-question-answers.yml` 现为 cron + `issues:[labeled,closed]` 双触发 + concurrency + 单 issue 手动输入 → 答案同步 ≤24h → 秒级。本地缺该提交（远程有）。
- Intake 队列：近 5 天 ~8 条真实 intake（#1458/#1460/#1472/#1473/#1477/#1427/#1418/#1459-question）停在 pending-review 等人工 AC；#1407 为 SMOKE TEST 占位待关。

## 待办 / 挂起

### 🟡 工程（等作者）
- **#1414**：Node 矩阵修复 + 末提交补签 → 合并 → 关 #1403。
- **#1469/#1470**：ghzhost 补 DCO → 合并 → 关 #1463/#1464（轮作闭环收尾）。
- 老一批：#1455/#1443（rebase，与 #1456 冲突）、#1430（#1071 互补）、#1399/#1437/#1451/#1452/#1454（lesson gate）、#1412/#1413/#1415（windows/lint）、#1400/#1411/#1461（fork DCO）、#1437。

### 🟢 维护者侧
- **pending intake 人工 AC**：#1407 关（SMOKE 占位）；#1458/#1460/#1472/#1473/#1477 判读（lesson 化 or 回问）；#1459 [Question] 回问详情。
- #1418（dsh-plugin-hub npm 登记跟进）、Glama claim connector（解锁 analytics）、README "385+" 口径（实际 lessons.json 索引 324+，营销虚高，暂缓）。
- PRD⑤：answered ≥10 或季度 → FAQ 语料编译（B/C 触发条件未达）；#1396/#1397/#1459 question 待答。
- 本地落后远程 → fetch+reset 对齐（勿 force-push）。

## 运维知识（本会话新增可复用）

- **release PR 流程**（每次）：release-please 开的 PR 是单提交无 sign-off → ① `/fix-dco` 对「与 base 无分叉单提交」是 **no-op**（勿依赖）② Git Data API amend 补 `Signed-off-by: Ikalus1988 <136884451+...>` ③ force-push `release-please--branches--main` ④ mergeable 轮询后 PUT squash ⑤ label `autorelease: tagged`（去掉 pending/needs-dco）⑥ POST /git/tags（annotated，用完整 sha）+ POST /releases ⑦ 版本对齐：server.json/glama×2/worker `MCP_VERSION ||` 默认 → 新版本（package.json = npm 轨保持不动）。merge 后 405 "Base branch was modified" = 需等 mergeable 重算再试。
- **CodeQL**：default setup 的 custom config 无 REST；advanced workflow `config-file:` + 顶层 `paths-ignore` 是可靠排除法；`query-filters exclude` 对 interprocedural taint 规则实测无效；alerts dismiss API 注释 ≤280 字符、reason 用 `false positive`。
- main 无强制 CI 门禁（仅 enforce_admins）→ 合并靠维护者判断；`audit` 检查全 PR 红为预存噪音（非阻塞）；lesson `gate` 是真信号。

## ⚠️ 风险提示

- **本地-远程 hash 分叉 + 本地落后**（缺外部会话提交如 #1471）；git 协议仍不稳 → 变更走 Git Data API（push_any 脚本模式）。
- 本轮 merge 均为 API squash，无 fresh-green 保证；watch main CI。
- CodeQL dismiss 后同 finding 若重现会新建 alert——fatal-guard 注入类若再现，考虑 paths-ignore 或接受周期性 dismiss。

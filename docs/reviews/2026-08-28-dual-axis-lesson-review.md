# Dual-Axis Lesson Review — 2026-08-28

## Summary

| Metric | Value |
|--------|-------|
| Groups reviewed | 10 / 10 |
| Files reviewed | 320 |
| Total findings | 555 |
| Critical | 87 |
| Major | 136 |
| Minor | 332 |

**By category:** frontmatter=169, verification=188, root_cause=26, sections=83, other=26, actionability=17, correctness=35, naming=8, redaction=3

**Review method:** 10 parallel agents (workflow fan-out), each reviewing ~30-40 lessons across two axes:
- **Standards axis** — frontmatter validity, section structure, verification presence, naming, redaction
- **Spec axis** — root-cause depth, solution actionability, factual correctness

## Systemic Findings by Group

### contrib-a (33 files, 43 findings)

6/33 files are clean (accidental-pycache-commit, aider-api-key-leak, bge-embedding-fallback-crash, bm25-chinese-sliding-window-tuning, bm25-vector-hybrid-search-weights, bounty-contributors-are-not-always-users). Systemic patterns: (1) ~13 files keep provenance inside the frontmatter instead of the <!-- provenance --> HTML-comment convention used by the JSON-frontmatter files; (2) most files end with a generic 'echo Lesson + wc -l' verification template that never verifies the actual fix — only a few files have real fix-verification (asyncio-cancellederror, aider-api-key-leak, accidental-pycache-commit); (3) 7 files carry broken `'{"title"': ...` JSON-injection frontmatter artifacts (the 'metadata-normalized' pattern), which pollute metadata and duplicate frontmatter blocks; (4) anthropic-proxy-internal-gateway and api-gateway-anthropic-incompatibility document the same incident and could be merged. Redaction is OK: all key-like strings in aider-api-key-leak are X-masked placeholders, and tokens in chrome-relay are <your-token> placeholders.

### contrib-b (33 files, 55 findings)

Systemic issues across the group (not repeated per file): (1) ~15 lessons use a placeholder verification template `echo "Lesson: <truncated title>"` + `wc -l`, which verifies nothing about the fix — all flagged major individually; (2) ~8 lessons have JSON+YAML mixed frontmatter (JSON object + trailing YAML `provenance:` block) — flagged major individually; (3) 3 assigned files do not exist under lessons/contrib/ and were reviewed at their actual locations under lessons/core/ (contributor-engagement-retention.md, cronjob-one-shot-race-condition-duplicate-execution.md, dco-auto-fix-workflow.md) — the assignment paths should be corrected; (4) provenance is placed inside the frontmatter in virtually all files despite the 'provenance outside frontmatter' rule — treated as a repo-wide convention and not flagged per file; (5) cleanest files: cloudflare-worker-deploy-three-pitfalls.md (real curl verification + expected output) and fanuc-backup-payload-extraction.md (deep, actionable, only a weak grep verification).

### contrib-c (33 files, 39 findings)

Systemic patterns: (1) 20 of 33 drafts place Problem/Root Cause/Solution at H3 under an H2 title instead of top-level ## Problem / ## Root Cause / ## Solution used by lessons/TEMPLATE.md and core lessons (flagged minor per file). (2) Most files end with a placeholder `## Verification` running `grep -i fanuc lessons/contrib/... | wc -l` that verifies nothing about lesson content; core lessons use topic-specific verification — flagged only where it is the sole verification. (3) Single-line JSON + YAML provenance frontmatter matches the repo's canonical core-lesson style, so it is not flagged per se; only the broken stray JSON in fanuc-r-2000ic-retrieval-fix.md and the multi-line JSON mix in fatal-guard-cli-hardening.md are flagged. (4) Redaction is clean across all files (no tokens/keys; the documented factory-default DCS password 1111 is standard FANUC documentation, not a secret). Strongest lessons on the spec axis: fanuc-kl-bytes-ahead (3 concrete verification methods), fanuc-kl-mm-module-h (topic-specific verification, no boilerplate), fanuc-kl-err-abort-vs-err-pause (deep root cause). fanuc-python-interface-fanucpy.md also has a truncated 'Python 3.'/'3.' expected-output placeholder.

### contrib-d (33 files, 50 findings)

Reviewed all 33 assigned files. 4 files are clean (git-clean-branch-after-merged-pr-ru, git-credential-helper-gh-path-mismatch, git-credentials-automation, git-merge-conflict-resolution). Systemic issues: (1) 15 files have broken frontmatter — duplicate `---` blocks and/or malformed `'{"title"':` JSON injection lines / JSON+YAML mixed inside one block (feishu-bot-setup-complete, feishu-doc-url-use-api-return, feishu-mcp-server-deepseek-tui-setup, feishu-upload-file-type-opus, feishu-webhook-url-env-config, ffmpeg-audio-libopus-not-ogg, firewall-port-open-not-public, game-mcp-* x3, feishu-websocket-404, fiverr-perimeterx, freellmapi, ghostty) — looks like a botched metadata normalization; recommend a repo-wide cleanup. (2) Nearly every file embeds provenance INSIDE frontmatter (rubric requires outside) and (3) ends with the same placeholder Verification block (`grep -i feishu|mcp ... | wc -l`, `echo ... verified`, or generic `git status/log`) that does not verify the fix — affected: all feishu-*, game-mcp-*, gfw-*, gateway-hang, firewall, fiverr, ffmpeg, ghostty, ghidra, git-credentials-*; only frontmatter-parsing-edge-cases, git-clean-branch, git-credential-helper have real checks. (4) Cross-file factual conflicts: batch limit ~20 (feishu-block-batch-limit) vs ~50 (feishu-block-api-false-success); heading block_type 1770001 (feishu-block-type-values-limits) vs heading=3 (feishu-block-api-false-success note) vs official docs 3–11. (5) Near-duplicates: gfw-tls-sni-block-pattern vs gfw-tls-sni-blocking-tool-layer-ineffective (same content EN/ZH); game-mcp trio shares structure. (6) `domain: contrib` used as a domain value in several files (ffmpeg, firewall, gateway). (7) Path discrepancy: freellmapi-session-context-mixing-cross-thread-delivery.md was assigned under lessons/contrib/ but the actual file lives in lessons/core/ (no contrib copy exists); findings reference the real path. (8) ghidra-mcp is a tool overview rather than a failure lesson. No secrets or hardcoded credentials found in any file; dates are consistently future-dated (2026), likely repo-internal convention.

### contrib-e (31 files, 83 findings)

2 of the 33 assigned files are NOT present under lessons/contrib/ — `github-actions-code-injection.md` and `hermes-state-database-lock-issues-cleanup-protocol.md` both exist only under `lessons/core/` (moved after merge), so files_reviewed=31. Two systemic patterns: (1) 9 files contain a broken `'{"title"': '...'` JSON injection AND a duplicate second frontmatter block (legacy metadata-normalization artifact, both critical); (2) 10 files mix a JSON frontmatter object with a YAML `provenance:` block — verified this fails YAML parsing (PyYAML ParserError), rated major. Also pervasive: the `## Verification` section in ~20 files is a template placeholder (`git status --short | head -5` / `echo`+`wc -l`) with `# (status)`-style expected output that does not verify the documented fix — rated major per file. Content/technical quality is otherwise strong (deep root causes, concrete commands). Also note near-duplicate lesson content between `github-api-pr-submission-pitfalls.md` and `github-contents-api-pr-pitfalls.md`, and the GnuTLS/IP workaround duplicated in `git-push-without-shell-agent.md` vs `github-dns-443-block-hosts-workaround.md`.

### contrib-f (33 files, 45 findings)

Systemic patterns across group contrib-f: (1) 8 files contain the broken `'{\"title\"':` JSON-injection plus a duplicated YAML metadata block after the frontmatter closing `---` (knowledge-graph-ux-patterns, lesson-06/07/08/09/10-doctor, lesson-file-line-number-corruption, lessons-md-fix-heading-block-type) — looks like a failed metadata-migration artifact and blocks parsing; (2) 4 files mix a JSON object with YAML `provenance:` in one frontmatter block (lesson-provenance-tracking, lesson-template-network, macos-...-pep-668, maintainer-feedback-iteration) — invalid for both JSON and YAML parsers; (3) most ## Verification sections only echo `wc -l` (or grep/search scripts), so they cannot confirm the fix works — flagged as minor where it is the only gap; (4) `provenance:` inside frontmatter is uniform across all 33 files and appears to be the repo's accepted convention (backfill_provenance.py writes it there), so it was not flagged per-file; (5) several specs-major findings are unverifiable commands copied from AI-summarized articles (ECS metrics enablement, EKS rollback command flags, MCP Python SDK API, Cloudflare ai-crawl endpoint) — worth a fact-check pass against official docs before publishing. Verified via web search that EKS cluster version rollback and Cloudflare Workflows saga rollback are real features (https://docs.aws.amazon.com/eks/latest/userguide/rollback-cluster.html, https://blog.cloudflare.com/rollbacks-for-workflows/), so those findings are limited to command/API accuracy, not feature existence.

### contrib-g (33 files, 62 findings)

Systemic patterns across the group: (1) ~20 files use placeholder verification (echo "Lesson: ..." + wc -l, or git status/log) that cannot confirm the fix — reported per file as minor; (2) 9 files mix JSON frontmatter with a trailing YAML provenance block inside ---, which fails json.loads and schema validation (major); (3) 5 files contain a broken '{"title"' JSON injection, 4 of them with a duplicated second frontmatter block (critical). pr-cleanup-sop.md was assigned as lessons/contrib/ but only exists at lessons/core/pr-cleanup-sop.md — reviewed at the actual path. Domain values 'contrib'/'openclaw' are non-canonical (index.md normalizes them to uncategorized) but content-domain fit is otherwise reasonable, so not flagged per file. mcp-endpoint-404-zone-route-misconfig.md is fully clean. mcp-server-direct-handler-testing.md, openclaw-playwright-wsl-libnss3-libnspr4-snap-chromium.md, mcporter-oauth-must-be-serial.md and the misakanet-* lessons have the strongest root-cause depth and actionable solutions.

### contrib-h (33 files, 64 findings)

33 files reviewed (31 under lessons/contrib/; pull-request-welcome-trigger-trap.md and search-first-roadmap-loop.md do not exist under lessons/contrib/ — they live in lessons/core/, reviewed there). Systematic issues across the group: (1) 14 files have JSON-object + YAML-provenance mixed frontmatter that is invalid YAML (critical); (2) 5 files carry the broken '{"title"' JSON injection plus a duplicate --- frontmatter block (major); (3) ~20 files have boilerplate ## Verification (python3 --version / echo+wc / grep rag-*.md) that does not verify the fix; (4) 7 rag files open Root Cause with a template sentence ('Inspect the RAG config, ingestion log...'). No secrets found in any file (smtplib uses placeholder app-password values). Strongest files: python-venv-tiktoken-module-not-found (real fix-specific verification), rag-cross-encoder-cpu-bottleneck and pull-request-welcome-trigger-trap (deep root cause, concrete fixes).

### contrib-i (48 files, 89 findings)

27 of 48 files have broken frontmatter from two systematic corruption families, almost certainly a batch metadata-normalization artifact: (A) 15 files carry a corrupted JSON-injection line `'{"title"': '...'` (mismatched quotes) plus a second duplicated `---` metadata block mid-file; (B) 12 files append YAML `provenance:` keys after a JSON object block, making the frontmatter parse as neither JSON nor YAML (verified with json.loads and yaml.safe_load). ~30 files also end with boilerplate verification (echo+`wc -l` with `# (line count)`, or `grep bm25/mcp/fanuc ... verified` with `# (refs)`) that never validates the lesson's fix; three files (tts-chinese-encoding-powershell, windows-ci-splitcommand-backslash-unicode-detached, wxauto-windows-python-not-wsl) have verifications entirely unrelated to their content (python version checks). wsl-terminal-underscore-corruption.md leaks a real `sk-cp-6L1Zvi...` credential fragment. wsl-terminal-underscore-corruption and wsl-terminal-underscore-missing are near-duplicate lessons on the same paste/underscore issue. Provenance-inside-frontmatter was NOT flagged since lessons/TEMPLATE.md itself embeds JSON metadata in frontmatter (repo norm).

### core (10 files, 25 findings)

Systemic: all 10 files share the legacy frontmatter pattern (JSON object + YAML provenance inside the --- fence). update_lessons_json.py tolerates it via raw_decode (documented legacy quirk), but lesson_gate.py's strict json.loads fails on it and the 2026-08+ convention is pure YAML — flagged per-file as minor. No hardcoded secrets found in any file; filenames are valid slugs; tags are proper arrays. Strongest lessons: cronjob-one-shot-race-condition (good root cause + concrete verification with command), pull-request-welcome-trigger-trap, search-first-roadmap-loop (matches Problem→Root Cause→Solution→Verification order). Weakest: freellmapi-session-context-mixing (fix is advice, not steps). auto-merge-ci-pipeline has two copy-paste-breaking code bugs (undefined RATIO; .mergeable boolean vs 'MERGEABLE' string) that should be fixed before the snippet is trusted.

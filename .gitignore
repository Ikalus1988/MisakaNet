# Python
__pycache__/
*.pyc
.pytest_cache/
.cache/
node_modules/
misakanet/profile.json
misakanet/.quota.json
*.pyo
venv/
*.egg-info/
dist/
build/

# Environment
.env
*.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# RAG runtime data
rag_chromadb/
audit_reports/
*.pkl

# Large files
*.rar
*.zip
*.7z
*.exe
scripts/_*.py
workers/wrangler.api.jsonc
.wrangler/
.dev.vars*
!.dev.vars.example
!.env.example

# Local agent/workflow runtime artifacts
.coverage
.hydra/
.worktrees/
data/sag.db

# Local toolchain (agent session caches, OAuth vaults — machine-specific)
.tools/
.tmp-cf/
.nodes/
.npm-cache/
.pnpm-store/
.agent-reach-home/
.cursor/mcp.json

# Session handoff doc (contains credential hints — keep local)
docs/prd/HANDOFF.md

# Local status snapshot (deliberately removed from repo in ed0316f9)
STATUS.md

# Crawler flywheel metrics (local tracking)
data/crawler_flywheel.jsonl

# Hub runtime/generated artifacts (regenerable, not source-of-truth)
hub/storage/audit.jsonl
hub/storage/skill_index.json
# Hub orchestrator artifacts produced at startup (sqlite databases)
storage/

# Usage meter runtime data (local tracking)
data/usage_credits.jsonl
data/contribution_queue.jsonl

# local search quality feedback (issue #604)
data/search-feedback.jsonl

# Zero-result search gap logging (issue #1175)
data/search_gaps.jsonl
.private/

# Benchmark results (local only)
bench_results/
.aider*
.aider*
coverage.xml
data/lessons.db

# Federation secrets (P0 security)
hub/federation/secrets/
